# GameSave Scout 批量入口与浅层扫描完成反馈实施计划

> 实施状态：2026-08-17 已完成。最终自动门禁为 Python 623 项、前端 29 个测试文件共 104 项，并通过 Ruff、mypy、Vue 类型检查、生产构建、schema 1 启动冒烟及 31 份 Markdown 本地链接检查。

> **供智能代理执行：** 必须使用 `superpowers:executing-plans` 按任务顺序实施；本项目明确不使用子代理。步骤使用复选框（`- [ ]`）跟踪。

**目标：** 把主页批量入口固定为“批量管理”在左、“批量封面”在右，并让空结果浅层扫描明确显示完成而不是继续显示正在收集候选。

**架构：** 前端只调整现有标题栏按钮的 DOM 顺序，不新增布局容器。浅层扫描继续复用通用 `TaskRegistry`，由桥接任务在取得 `LocalDiscoverySummary` 后补充来源专属的 `1/1` 最终进度和候选数量提示，避免前端解析不稳定的通用 `result` 结构。

**技术栈：** Vue 3、TypeScript、Vitest、Python 3.12、pytest、pywebview 桥接、Vite。

## 全局约束

- 直接在当前本地 `main` 工作，不创建分支或 worktree，不使用子代理。
- 不新增依赖，不修改 SQLite schema，不迁移数据库。
- 所有生产改动先写失败测试并确认 RED，再写最小实现并确认 GREEN。
- 修改和验证完成后直接提交 Git；不自动推送远端。
- 固定设计继续维护 `docs/superpowers/plans/2026-08-12-GameSave-Scout-03-封面与游戏库界面.md`；本文件只记录一次性实施过程。

---

### 任务 1：固定主页批量入口顺序

**文件：**

- 修改：`frontend/src/App.vue:237-243`
- 测试：`frontend/tests/App.spec.ts`

**接口：**

- 消费：现有 `data-test="enter-batch-mode"` 与 `data-test="enter-cover-wizard"` 入口。
- 产出：当两个入口同时显示时，`.compact-actions` 内的按钮文本顺序固定为 `['批量管理', '批量封面']`。

- [x] **步骤 1：添加按钮顺序失败测试**

在 `frontend/tests/App.spec.ts` 的 `App` 测试组中增加：

```ts
it('places batch management to the left of batch covers', async () => {
  const bridge = createMockBridge({
    async list_games() { return ok([fixtureGame()]) },
  })
  const wrapper = mount(App, {
    global: {
      plugins: [createPinia()],
      provide: { [bridgeKey as symbol]: bridge },
    },
  })
  await flushPromises()

  expect(wrapper.findAll('.compact-actions button').map((item) => item.text())).toEqual([
    '批量管理',
    '批量封面',
  ])
})
```

- [x] **步骤 2：运行测试并确认 RED**

运行：

```powershell
& .\.venv\npm.cmd --prefix frontend run test:unit -- --run tests/App.spec.ts
```

预期：新增断言失败，实际顺序为 `['批量封面', '批量管理']`。

- [x] **步骤 3：交换模板中的两个入口**

把 `frontend/src/App.vue` 的标题栏按钮改为：

```vue
<button v-if="!batchMode && removableGames.length" data-test="enter-batch-mode" class="secondary" type="button" @click="enterBatchMode">批量管理</button>
<button ref="coverWizardEntry" data-test="enter-cover-wizard" class="secondary" type="button" @click="openCoverWizard">批量封面</button>
```

不修改条件渲染、点击处理、焦点恢复或 CSS。

- [x] **步骤 4：运行任务 1 测试并确认 GREEN**

运行：

```powershell
& .\.venv\npm.cmd --prefix frontend run test:unit -- --run tests/App.spec.ts
```

预期：`App.spec.ts` 全部通过。

---

### 任务 2：为空浅层扫描上报明确完成状态

**文件：**

- 修改：`src/gamesave_scout/bridge/api.py:351-373`
- 测试：`tests/unit/bridge/test_cover_wizard_api.py`

**接口：**

- 消费：`LocalDiscoverySummary.candidates` 与 `TaskContext.report(completed, total, message)`。
- 产出：浅层扫描任务终态固定包含 `progress={"completed": 1, "total": 1}`；空结果消息为 `浅层扫描完成，未找到候选封面。`，非空结果消息为 `浅层扫描完成，找到 N 张候选封面。`。

- [x] **步骤 1：让测试假对象支持空浅层扫描结果**

在 `_Wizard` 增加可选字段并让 `collect_shallow` 优先返回它：

```python
shallow_summary: LocalDiscoverySummary | None = None

def collect_shallow(self, session_id, game_id, limit, context):
    del limit, context
    assert (session_id, game_id) == (self.state.id, self.game.id)
    if self.shallow_summary is not None:
        return self.shallow_summary
    return LocalDiscoverySummary((self.candidate,), 1, 0, False, ())
```

- [x] **步骤 2：添加空结果失败测试并加强非空断言**

在现有 `test_source_tasks_return_json_safe_counts` 中增加：

```python
assert task.progress == {"completed": 1, "total": 1}
assert task.message == "浅层扫描完成，找到 1 张候选封面。"
```

再增加：

```python
def test_empty_shallow_scan_reports_completed_without_candidates(tmp_path: Path) -> None:
    api, tasks, wizard = _api(tmp_path)
    wizard.shallow_summary = LocalDiscoverySummary((), 0, 0, False, ())
    try:
        result = api.start_cover_shallow_scan(
            {"sessionId": "wizard-1", "gameId": "game-1", "limit": 10}
        )
        task = tasks.wait(result["data"]["taskId"], timeout=5)

        assert task.status == "completed"
        assert task.progress == {"completed": 1, "total": 1}
        assert task.message == "浅层扫描完成，未找到候选封面。"
        assert task.result == {
            "sessionId": "wizard-1",
            "completedCount": 0,
            "failedCount": 0,
        }
    finally:
        tasks.close()
```

- [x] **步骤 3：运行测试并确认 RED**

运行：

```powershell
& .\.venv\python.exe -m pytest tests/unit/bridge/test_cover_wizard_api.py -q
```

预期：任务已完成，但当前进度仍为 `0/null` 且消息为空，因此新增断言失败。

- [x] **步骤 4：在桥接任务返回前上报最终状态**

在 `start_cover_shallow_scan` 的 `operation` 中使用候选数量生成最终消息：

```python
candidate_count = len(summary.candidates)
message = (
    "浅层扫描完成，未找到候选封面。"
    if candidate_count == 0
    else f"浅层扫描完成，找到 {candidate_count} 张候选封面。"
)
context.report(1, 1, message)
return {
    "sessionId": session_id,
    "completedCount": candidate_count,
    "failedCount": summary.skipped,
}
```

不修改通用 `TaskRegistry`，不让前端解析任务 `result`。

- [x] **步骤 5：运行任务 2 测试并确认 GREEN**

运行：

```powershell
& .\.venv\python.exe -m pytest tests/unit/bridge/test_cover_wizard_api.py -q
& .\.venv\npm.cmd --prefix frontend run test:unit -- --run tests/CoverCandidateToolbar.spec.ts tests/coverWizardStore.spec.ts
```

预期：桥接任务测试和现有完成进度渲染测试全部通过。

---

### 任务 3：文档状态、生产资源与完整质量门禁

**文件：**

- 修改：`docs/superpowers/plans/2026-08-12-GameSave-Scout-03-封面与游戏库界面.md`
- 修改：`docs/历史归档/2026-08/2026-08-17-批量入口与浅层扫描完成反馈实施计划.md`
- 重建：`resources/ui/index.html`
- 重建：`resources/ui/assets/*`

**接口：**

- 消费：任务 1、2 的已验证实现。
- 产出：与源码一致的内置前端资源、固定设计完成记录、完整质量门禁证据和 Git 提交。

- [x] **步骤 1：运行完整 Python 门禁**

```powershell
& .\.venv\python.exe -m pytest
& .\.venv\python.exe -m ruff check src tests scripts
& .\.venv\python.exe -m mypy src scripts
```

预期：全部 Python 测试通过，Ruff 和 mypy 无错误。

- [x] **步骤 2：运行完整前端门禁并重建生产资源**

```powershell
& .\.venv\npm.cmd --prefix frontend run test:unit -- --run
& .\.venv\npm.cmd --prefix frontend run type-check
& .\.venv\npm.cmd --prefix frontend run build
```

预期：全部前端测试通过，类型检查和生产构建成功，`resources/ui` 只出现预期哈希资源变化。

- [x] **步骤 3：运行源码冒烟、文档和差异检查**

```powershell
& .\.venv\python.exe -m gamesave_scout --smoke-test
git diff --check
git status --short
```

另外遍历 `README.md` 与 `docs/**/*.md` 的本地 Markdown 链接，预期失效链接为 0。

- [x] **步骤 4：更新完成状态**

- 在固定设计文档变更记录中追加本次实现完成记录和最终测试数量。
- 把本计划顶部改为“已完成”，勾选所有步骤，并记录完整门禁结果。

- [x] **步骤 5：提交全部实现、测试、资源和文档**

```powershell
git add docs frontend resources/ui src tests
git commit -m "fix: 完善批量入口与浅层扫描反馈"
```

预期：提交成功，`git status --short` 为空；不推送远端。
