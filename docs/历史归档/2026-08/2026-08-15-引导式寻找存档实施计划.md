# GameShelf 引导式寻找存档实施计划

> 执行状态：产品代码与自动验证已完成；任务 13 步骤 8 的真实 Windows 游戏、资源占用和多缩放人工验收待用户执行；全部修改保持未提交。

> **执行要求：** REQUIRED SUB-SKILL：使用 `superpowers:executing-plans` 在当前任务中逐项执行。禁止子代理、开发分支和 worktree；每个任务完成后保留未提交差异，只有用户明确要求时才提交 Git。

**目标：** 为 V0.1 实现用户主动开始、由 GameShelf 启动游戏、点击“我刚刚保存了”后约 3 秒自动分析并审核候选的引导式存档定位功能。

**架构：** `GuidedSaveSessionService` 维护全程序唯一活动会话，组合项目内 Win32 目录观察器、进程树跟踪器、定向注册表快照、受限补扫器、事件聚合器和确定性评分器。正常路径只消费 Windows 文件事件，不周期性遍历 AppData；最终候选以事务写入 SQLite，用户明确接受后才通过共享的存档位置持久化边界写入 `save_locations`。

**技术栈：** Python 3.12、`ctypes`/Win32、SQLite、pywebview、Vue 3、TypeScript、Pinia、Vitest；不新增运行时第三方依赖。

## 全局约束

- 仅支持 Windows 10/11 x64 和本机目录；UNC、网络共享、符号链接、目录联接及其他越界重解析点不进入 V0.1 监控范围。
- 现有静态“查找存档”保持按需触发；引导式寻找使用独立入口，只有用户点击“确认开始”才建立观察器和启动游戏。
- 观察器全部就绪后才能启动游戏；全程序最多一个 `preparing`、`monitoring` 或 `settling` 会话。
- 点击“我刚刚保存了”后继续观察约 3 秒并自动分析；无标记的自动存档流程使用“停止并分析”。
- 活动会话最长 30 分钟；不同变化路径上限 20,000，事件队列上限 50,000，过滤前候选上限 500，最终展示上限 200。
- 溢出补扫每个根目录最多 8 层、100,000 个条目、5 秒，只读取路径、创建/修改时间和大小。
- 不读取存档文件内容，不持久化原始事件流或注册表原始值，不声称某个 PID 写入了某个文件。
- 额外监控目录必须能折叠为现有便携令牌；不扩展绝对路径存档模板。
- 直接修改 `0001_initial.sql`，schema 版本保持 1；旧开发数据库先移动到 `data/db_backups` 的独立时间戳目录，再重新建库。
- 仓库内设计与开发文档使用中文；施工计划保存在 `docs/历史归档`。
- 当前 README 中的 Codex 审核测试注释与本功能无关，始终保持未暂存、未提交。
- 每个任务遵循测试先行：先观察目标测试失败，再写最小实现，再运行局部和相关回归测试。
- 不执行自动 Git 提交。任务末尾只运行 `git diff --check` 和 `git status --short` 作为审核检查点。

---

## 文件结构

### 后端新增文件

| 文件 | 单一职责 |
| --- | --- |
| `src/gameshelf/saves/guided_models.py` | 会话、范围、事件、候选和桥接边界的不可变类型 |
| `src/gameshelf/saves/guided_repository.py` | 会话与候选的短连接查询和 `DbWriter` 事务 |
| `src/gameshelf/saves/guided_scope.py` | 默认范围、额外目录、便携模板、去重和定向注册表键解析 |
| `src/gameshelf/saves/guided_events.py` | 有上限的线程安全事件聚合和原子重命名归并 |
| `src/gameshelf/saves/guided_scanner.py` | 仅用于溢出/失败范围的元数据补扫 |
| `src/gameshelf/saves/guided_registry.py` | 定向注册表元数据快照、摘要和差异 |
| `src/gameshelf/saves/guided_scoring.py` | 噪声过滤、目录合并、确定性评分和默认选择 |
| `src/gameshelf/saves/guided_review.py` | 候选审核、注册表确认和事务式正式位置写入 |
| `src/gameshelf/saves/guided_service.py` | 单活动会话状态机、计时器、观察器、进程和分析编排 |
| `src/gameshelf/saves/location_persistence.py` | 共享的已确认存档位置准备与连接内 upsert |
| `src/gameshelf/platform/windows/directory_watcher.py` | `ReadDirectoryChangesW` 适配、缓冲区解析、溢出和可取消关闭 |
| `src/gameshelf/platform/windows/process_tree.py` | Tool Help 进程快照和根 PID/子进程生命周期跟踪 |

### 前端新增文件

| 文件 | 单一职责 |
| --- | --- |
| `frontend/src/features/saves/guidedSaveStore.ts` | 活动会话、低频轮询、命令和恢复详情请求 |
| `frontend/src/features/saves/GuidedSavePanel.vue` | 范围确认及 preparing/monitoring/settling 状态界面 |
| `frontend/src/features/saves/GuidedSaveDiscoveries.vue` | 完成候选分组、证据、确认、接受和丢弃 |
| `frontend/src/features/saves/GuidedSaveStatusBar.vue` | 详情关闭后的主页活动状态栏 |
| `frontend/src/features/saves/GuidedSaveCloseDialog.vue` | 窗口关闭请求的停止分析、取消退出和返回程序选择 |

### 主要修改文件

- `src/gameshelf/db/migrations/0001_initial.sql`
- `src/gameshelf/saves/models.py`
- `src/gameshelf/saves/service.py`
- `src/gameshelf/saves/static_discovery.py`
- `src/gameshelf/platform/windows/registry.py`
- `src/gameshelf/bootstrap/application.py`
- `src/gameshelf/bridge/api.py`
- `src/gameshelf/app.py`
- `frontend/src/api/contracts.ts`
- `frontend/src/api/mockBridge.ts`
- `frontend/src/App.vue`
- `frontend/src/features/library/GameGrid.vue`
- `frontend/src/features/saves/SaveLocationList.vue`
- `frontend/src/features/library/library.css`
- `docs/superpowers/plans/2026-08-12-GameShelf-06-动态与孤立存档发现.md`
- `docs/superpowers/plans/2026-08-12-GameShelf-开发路线图.md`

---

### 任务 1：冻结初始数据库结构、模型和仓库

**文件：**

- 修改：`src/gameshelf/db/migrations/0001_initial.sql`
- 新建：`src/gameshelf/saves/guided_models.py`
- 新建：`src/gameshelf/saves/guided_repository.py`
- 修改：`tests/unit/db/test_migrator.py`
- 新建：`tests/unit/saves/test_guided_repository.py`

**接口：**

- 产出：`GuidedSaveSession`、`GuidedSaveDiscovery`、`GuidedScopeOption`、`GuidedSavePreview`、`GuidedSessionStatus`。
- 产出：`GuidedSaveRepository.create_session()`、`set_monitoring()`、`mark_settling()`、`set_process_tracking_degraded()`、`complete()`、`fail()`、`cancel()`、`recover_interrupted()`、`active()`、`get_session()`、`latest_reviewable(game_id: str | None = None)`、`list_discoveries()`、`discard()`。
- 后续任务只通过这些模型和仓库方法访问会话表，不在服务中散写 SQL。

- [x] **步骤 1：先写最终 schema 的失败测试**

在 `tests/unit/db/test_migrator.py` 增加断言，检查活动槽、状态、候选路径键和正式位置外键：

```python
def test_v1_guided_save_schema_has_single_active_slot_and_review_fields(
    tmp_path: Path,
) -> None:
    factory = ConnectionFactory(tmp_path / "library.db")
    Migrator(factory, tmp_path / "backups").migrate()

    with factory.connect() as connection:
        session_sql = connection.execute(
            "SELECT sql FROM sqlite_master WHERE name = 'save_detection_sessions'"
        ).fetchone()[0]
        discovery_columns = {
            row[1] for row in connection.execute("PRAGMA table_info(save_discoveries)")
        }
        indexes = {
            row[1] for row in connection.execute("PRAGMA index_list(save_detection_sessions)")
        }

    assert "interrupted" in session_sql
    assert "active_slot" in session_sql
    assert "save_detection_one_active" in indexes
    assert {
        "path_key",
        "representative_files_json",
        "first_changed_at",
        "last_changed_at",
        "mark_offset_ms",
        "affected_by_overflow",
        "affected_by_truncation",
        "preselected",
        "save_location_id",
    } <= discovery_columns
```

- [x] **步骤 2：运行 schema 测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/db/test_migrator.py -v
```

预期：新增断言因缺少 `active_slot`、`interrupted` 或候选字段失败。

- [x] **步骤 3：将预留表改成最终初始结构**

在 `0001_initial.sql` 中使用以下字段和约束；保留未来 V0.2 所需的 `scan_session_id`、`suggested_game`、`engine_id` 和 `linked_game_id`：

```sql
CREATE TABLE save_detection_sessions (
  id TEXT PRIMARY KEY,
  game_id TEXT NOT NULL REFERENCES games(id) ON DELETE CASCADE,
  status TEXT NOT NULL CHECK (status IN (
    'preparing', 'monitoring', 'settling', 'completed',
    'cancelled', 'failed', 'interrupted'
  )),
  active_slot INTEGER CHECK (active_slot IS NULL OR active_slot = 1),
  started_at TEXT NOT NULL,
  monitoring_started_at TEXT,
  save_marked_at TEXT,
  finished_at TEXT,
  root_pid INTEGER,
  approved_scopes_json TEXT NOT NULL DEFAULT '[]',
  unavailable_scopes_json TEXT NOT NULL DEFAULT '[]',
  overflowed_scopes_json TEXT NOT NULL DEFAULT '[]',
  truncated_scopes_json TEXT NOT NULL DEFAULT '[]',
  process_tracking_degraded INTEGER NOT NULL DEFAULT 0
    CHECK (process_tracking_degraded IN (0, 1)),
  result_summary_json TEXT NOT NULL DEFAULT '{}',
  error_code TEXT,
  error_summary TEXT,
  CHECK (
    (status IN ('preparing', 'monitoring', 'settling') AND active_slot = 1)
    OR (status NOT IN ('preparing', 'monitoring', 'settling') AND active_slot IS NULL)
  )
);

CREATE UNIQUE INDEX save_detection_one_active
  ON save_detection_sessions(active_slot)
  WHERE active_slot = 1;

CREATE TABLE save_discoveries (
  id TEXT PRIMARY KEY,
  scan_session_id TEXT REFERENCES scan_sessions(id) ON DELETE CASCADE,
  detection_session_id TEXT REFERENCES save_detection_sessions(id) ON DELETE CASCADE,
  candidate_template TEXT NOT NULL,
  display_path TEXT NOT NULL,
  path_key TEXT NOT NULL,
  kind TEXT NOT NULL CHECK (kind IN ('directory', 'file', 'registry')),
  suggested_game TEXT,
  engine_id TEXT,
  confidence REAL NOT NULL CHECK (confidence >= 0 AND confidence <= 1),
  evidence_json TEXT NOT NULL DEFAULT '[]',
  representative_files_json TEXT NOT NULL DEFAULT '[]',
  first_changed_at TEXT,
  last_changed_at TEXT,
  mark_offset_ms INTEGER,
  affected_by_overflow INTEGER NOT NULL DEFAULT 0
    CHECK (affected_by_overflow IN (0, 1)),
  affected_by_truncation INTEGER NOT NULL DEFAULT 0
    CHECK (affected_by_truncation IN (0, 1)),
  preselected INTEGER NOT NULL DEFAULT 0 CHECK (preselected IN (0, 1)),
  review_status TEXT NOT NULL DEFAULT 'unreviewed'
    CHECK (review_status IN ('unreviewed', 'accepted', 'ignored', 'linked', 'save_only')),
  linked_game_id TEXT REFERENCES games(id) ON DELETE SET NULL,
  save_location_id TEXT REFERENCES save_locations(id) ON DELETE SET NULL,
  CHECK (
    (scan_session_id IS NOT NULL AND detection_session_id IS NULL)
    OR (scan_session_id IS NULL AND detection_session_id IS NOT NULL)
  ),
  UNIQUE(detection_session_id, kind, path_key)
);
```

- [x] **步骤 4：定义不可变模型和 JSON 边界**

`guided_models.py` 至少定义以下类型，所有 JSON 数组在仓库边界转换为元组：

```python
type GuidedSessionStatus = Literal[
    "preparing", "monitoring", "settling", "completed",
    "cancelled", "failed", "interrupted",
]
type GuidedScopeSource = Literal[
    "game", "documents", "saved_games", "app_data", "local_app_data",
    "local_app_data_low", "program_data", "confirmed", "extra",
]

@dataclass(frozen=True, slots=True)
class GuidedScopeOption:
    id: str
    label: str
    display_path: str
    path_template: str
    source: GuidedScopeSource
    default_selected: bool
    available: bool
    unavailable_reason: str | None = None

@dataclass(frozen=True, slots=True)
class GuidedRegistryTarget:
    key: str
    source: str
    available: bool

@dataclass(frozen=True, slots=True)
class GuidedSavePreview:
    game_id: str
    game_title: str
    executable: str
    scopes: tuple[GuidedScopeOption, ...]
    registry_targets: tuple[GuidedRegistryTarget, ...]

@dataclass(frozen=True, slots=True)
class GuidedSaveSession:
    id: str
    game_id: str
    status: GuidedSessionStatus
    started_at: str
    monitoring_started_at: str | None
    save_marked_at: str | None
    finished_at: str | None
    root_pid: int | None
    approved_scopes: tuple[GuidedScopeOption, ...]
    unavailable_scopes: tuple[str, ...]
    overflowed_scopes: tuple[str, ...]
    truncated_scopes: tuple[str, ...]
    process_tracking_degraded: bool
    result_summary: dict[str, int]
    error_code: str | None
    error_summary: str | None

@dataclass(frozen=True, slots=True)
class GuidedSaveDiscovery:
    id: str
    detection_session_id: str
    candidate_template: str
    display_path: str
    path_key: str
    kind: Literal["directory", "file", "registry"]
    confidence: float
    evidence: tuple[str, ...]
    representative_files: tuple[str, ...]
    first_changed_at: str | None
    last_changed_at: str | None
    mark_offset_ms: int | None
    affected_by_overflow: bool
    affected_by_truncation: bool
    preselected: bool
    review_status: Literal["unreviewed", "accepted", "ignored"]
    save_location_id: str | None

@dataclass(frozen=True, slots=True)
class GuidedDiscoveryDraft:
    candidate_template: str
    display_path: str
    path_key: str
    kind: Literal["directory", "file", "registry"]
    confidence: float
    evidence: tuple[str, ...]
    representative_files: tuple[str, ...]
    first_changed_at: str | None
    last_changed_at: str | None
    mark_offset_ms: int | None
    affected_by_overflow: bool
    affected_by_truncation: bool
    preselected: bool
```

- [x] **步骤 5：先写仓库状态和唯一会话失败测试**

`tests/unit/saves/test_guided_repository.py` 覆盖：创建会话、第二个活动会话触发稳定异常、进入 monitoring、进入 settling、事务完成候选、恢复 interrupted、按全库或游戏读取最近未审核完成会话、丢弃后不再被 `latest_reviewable()` 返回。测试必须断言终态 `active_slot IS NULL`。

- [x] **步骤 6：实现 `GuidedSaveRepository`**

使用 `ConnectionFactory` 读取、`DbWriter` 写入。定义稳定异常：

```python
class ActiveGuidedSessionError(RuntimeError):
    pass

class GuidedSessionNotFoundError(LookupError):
    pass

class InvalidGuidedSessionState(RuntimeError):
    pass
```

`complete()` 在一个 `DbWriter` 操作中删除本会话旧的未审核候选、批量插入新候选、更新终态和统计；任何 SQLite 错误由 writer 事务整体回滚。

- [x] **步骤 7：运行任务 1 测试和审核检查点**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/db/test_migrator.py tests/unit/saves/test_guided_repository.py -v
.\.venv\Scripts\python.exe -m ruff check src/gameshelf/saves/guided_models.py src/gameshelf/saves/guided_repository.py tests/unit/saves/test_guided_repository.py
git diff --check
git status --short
```

预期：测试通过；README 仍只显示为未暂存的既有审核注释。

---

### 任务 2：构建可解释、可重新验证的监控范围预览

**文件：**

- 新建：`src/gameshelf/saves/guided_scope.py`
- 修改：`src/gameshelf/saves/static_discovery.py`
- 新建：`tests/unit/saves/test_guided_scope.py`
- 修改：`tests/integration/saves/test_static_discovery.py`

**接口：**

- 消费：`LibraryService.get_game()`、`install_directory()`、`KnownFolders`、`SaveLocationRepository`、`PathTemplateResolver`。
- 产出：`GuidedSaveScopeBuilder.preview(game_id)` 和 `resolve_selected(game_id, selected_scope_ids, additional_directories)`。
- 产出：`StaticSaveDiscovery.registry_targets_for_game(game_id)`，只返回当前游戏静态建议中的注册表键和证据。

- [x] **步骤 1：先写默认范围和拒绝边界测试**

测试至少包含：游戏目录、Documents、Saved Games、Roaming、Local、LocalLow 默认选中；ProgramData 默认不选；确认位置父目录去重；UNC 拒绝；不存在目录不可用；额外目录无法折叠为便携模板时拒绝；选中 ID 在确认后失效时整体失败。

```python
def test_preview_marks_program_data_optional_and_deduplicates_nested_roots(
    scope_builder: GuidedSaveScopeBuilder,
) -> None:
    preview = scope_builder.preview("game-1")
    by_source = {scope.source: scope for scope in preview.scopes}

    assert by_source["game"].default_selected is True
    assert by_source["local_app_data"].default_selected is True
    assert by_source["program_data"].default_selected is False
    assert len({scope.path_template.casefold() for scope in preview.scopes}) == len(
        preview.scopes
    )
```

- [x] **步骤 2：运行范围测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_scope.py -v
```

预期：因 `GuidedSaveScopeBuilder` 不存在而失败。

- [x] **步骤 3：增加静态注册表目标查询**

在 `StaticSaveDiscovery` 中增加：

```python
def registry_targets_for_game(
    self, game_id: str
) -> tuple[tuple[str, tuple[str, ...]], ...]:
    return tuple(
        (suggestion.path_template, suggestion.evidence)
        for suggestion in self.suggest_for_game(game_id)
        if suggestion.kind == "registry"
    )
```

保持按需语义：只有用户打开引导式范围预览才调用；应用启动、详情打开和存档区域展开均不得调用。

- [x] **步骤 4：实现默认范围、便携性和去重**

`GuidedSaveScopeBuilder` 使用稳定 ID：`default:game`、`default:documents`、`default:saved-games`、`default:roaming`、`default:local`、`default:local-low`、`default:program-data`、`confirmed:<location-id>`。额外目录在 `resolve_selected()` 中重新验证绝对本机路径、目录存在、非重解析点，并调用 `PathTemplateResolver.collapse()`；无法折叠时抛出 `InvalidGuidedScope("额外目录无法表示为便携存档路径。")`。

嵌套根目录按 Windows 路径键排序后只保留最外层；范围证据仍在会话 JSON 中记录被覆盖的来源。

- [x] **步骤 5：运行范围、静态发现与模板回归测试**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_scope.py tests/integration/saves/test_static_discovery.py tests/unit/saves/test_templates.py -v
git diff --check
```

预期：范围测试、静态按需测试和模板回归全部通过。

---

### 任务 3：实现有上限的事件聚合和溢出补扫

**文件：**

- 新建：`src/gameshelf/saves/guided_events.py`
- 新建：`src/gameshelf/saves/guided_scanner.py`
- 新建：`tests/unit/saves/test_guided_events.py`
- 新建：`tests/unit/saves/test_guided_scanner.py`

**接口：**

- 产出：`RawFileChange`、`AggregatedFileChange`、`GuidedChangeAggregator`。
- 产出：`BoundedMetadataScanner.scan(root, started_ns, finished_ns)` 返回 `MetadataScanResult`。
- 后续观察器只调用聚合器 `record()`、`mark_overflow()`、`mark_failure()`；评分器只读取不可变 `snapshot()`。

- [x] **步骤 1：先写事件上限、重复合并和重命名测试**

```python
def test_aggregator_merges_atomic_replace_into_final_path() -> None:
    aggregator = GuidedChangeAggregator(max_paths=20_000, max_events=50_000)
    aggregator.record(RawFileChange("created", Path(r"C:\Save\slot.tmp"), None, 10))
    aggregator.record(
        RawFileChange(
            "moved", Path(r"C:\Save\slot.tmp"), Path(r"C:\Save\slot.sav"), 11
        )
    )

    changes = aggregator.snapshot().changes

    assert [change.display_path for change in changes] == [r"C:\Save\slot.sav"]
    assert changes[0].operations == ("created", "moved")
```

另写测试把相同路径记录 50,001 次，断言事件计数封顶且根目录进入 overflow；记录 20,001 个不同路径时不扩容。

- [x] **步骤 2：运行聚合测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_events.py -v
```

预期：导入失败。

- [x] **步骤 3：实现线程安全聚合器**

使用 `RLock`、Windows `path_key` 和插入顺序序号。`record()` 不执行递归 I/O；移动事件将源路径操作迁移到目标路径。快照类型：

```python
@dataclass(frozen=True, slots=True)
class AggregatorSnapshot:
    changes: tuple[AggregatedFileChange, ...]
    event_count: int
    overflowed_roots: tuple[str, ...]
    failed_roots: tuple[str, ...]
    dropped_event_count: int
```

- [x] **步骤 4：先写补扫深度、条目、时间和重解析点测试**

在临时目录建立 9 层树、超过小型测试上限的文件、超出时间窗口的旧文件和一个 Windows 目录联接替身（单元测试通过注入 `is_reparse_point` 返回值）。断言只返回窗口内元数据，且 `truncated_by` 精确为 `depth`、`entries` 或 `deadline`。

- [x] **步骤 5：实现 `BoundedMetadataScanner`**

构造函数允许测试缩小上限：

```python
class BoundedMetadataScanner:
    def __init__(
        self,
        *,
        max_depth: int = 8,
        max_entries: int = 100_000,
        max_seconds: float = 5.0,
        monotonic: Callable[[], float] = time.monotonic,
        is_reparse_point: Callable[[Path], bool] = is_windows_reparse_point,
    ) -> None: ...

    def scan(
        self, root: Path, *, started_ns: int, finished_ns: int
    ) -> MetadataScanResult: ...
```

遍历使用 `os.scandir()`、显式栈和深度；每个条目前检查 deadline、计数、批准根边界和重解析点。只返回 `st_ctime_ns` 或 `st_mtime_ns` 落在会话窗口中的文件。

- [x] **步骤 6：运行聚合与补扫测试**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_events.py tests/unit/saves/test_guided_scanner.py -v
.\.venv\Scripts\python.exe -m ruff check src/gameshelf/saves/guided_events.py src/gameshelf/saves/guided_scanner.py tests/unit/saves/test_guided_events.py tests/unit/saves/test_guided_scanner.py
git diff --check
```

预期：全部通过且扫描器测试不会访问测试根以外的路径。

---

### 任务 4：实现噪声过滤、目录合并和确定性评分

**文件：**

- 新建：`src/gameshelf/saves/guided_scoring.py`
- 新建：`tests/unit/saves/test_guided_scoring.py`

**接口：**

- 消费：聚合快照、补扫结果、保存标记、范围来源、静态路径证据和已有正式位置。
- 产出：最多 200 个 `GuidedDiscoveryDraft`，按 `(-confidence, path_key)` 排序。

- [x] **步骤 1：先写代表性评分测试矩阵**

测试用固定纳秒时间覆盖：标记前后 2 秒、标记后 3 秒、无标记、同目录多文件、静态规则命中、缓存路径、原子替换、已有位置、溢出和截断。

```python
def test_save_mark_and_coordinated_files_rank_real_save_above_cache() -> None:
    drafts = score_guided_changes(
        changes=(
            change(r"C:\Users\Alice\AppData\LocalLow\Studio\Game\slot1.sav", 9_900),
            change(r"C:\Users\Alice\AppData\LocalLow\Studio\Game\meta.dat", 9_950),
            change(r"C:\Users\Alice\AppData\Local\Browser\Cache\entry.bin", 10_000),
        ),
        save_mark_ns=10_000,
        context=scoring_context(),
    )

    assert drafts[0].display_path.endswith(r"Studio\Game")
    assert drafts[0].confidence >= 0.85
    assert all("Browser\\Cache" not in item.display_path for item in drafts)
```

- [x] **步骤 2：运行评分测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_scoring.py -v
```

预期：导入失败。

- [x] **步骤 3：实现明确权重和证据文案**

使用以下固定权重，最后夹紧到 `0.0～1.0`：

- 基础会话变化：`0.10`。
- 距保存标记不超过 2 秒：`+0.55`；超过 2 秒但不超过 10 秒：`+0.30`。
- 同目录至少两个协调变化文件：`+0.15`。
- 与静态规则路径一致：`+0.20`。
- 位于游戏目录、确认位置父目录或可信引擎结构：`+0.10`。
- 文件名或组合具有存档特征：最多 `+0.05`。
- 明确日志/缓存/着色器/遥测/崩溃转储路径：过滤。
- 其他程序高频目录但不满足明确过滤：`-0.25`。

目录候选最多列出 8 个代表性文件。没有保存标记、受溢出影响或受截断影响时 `preselected=False`；否则仅 `confidence >= 0.85` 的文件系统候选预选。低于 `0.60` 的候选保留但标记为低可信。

- [x] **步骤 4：实现安全模板折叠和展示上限**

每个文件/目录候选必须通过 `PathTemplateResolver.collapse()`。无法折叠的变化不进入可接受候选，并在结果统计增加 `unportable`。先保留最高 500 个草稿，目录合并和过滤后只输出前 200 个。

- [x] **步骤 5：运行评分及静态模板回归测试**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_scoring.py tests/unit/saves/test_templates.py -v
git diff --check
```

预期：真实保存候选排序、阈值、默认选择和上限断言全部通过。

---

### 任务 5：实现定向注册表元数据快照

**文件：**

- 修改：`src/gameshelf/platform/windows/registry.py`
- 新建：`src/gameshelf/saves/guided_registry.py`
- 修改：`tests/unit/platform/windows/test_save_openers.py`
- 新建：`tests/unit/saves/test_guided_registry.py`

**接口：**

- 产出：`RegistryMetadataReader.snapshot(keys)`。
- 产出：`diff_registry_snapshots(before, after)`，结果可以转换成注册表 `GuidedDiscoveryDraft`。

- [x] **步骤 1：先写不持久化原始值的失败测试**

使用假的 `winreg` API 返回字符串、整数和二进制值。断言快照只有键名、值名、类型、长度和 SHA-256，序列化 JSON 中不出现原始秘密文本。

```python
def test_registry_snapshot_hashes_values_without_retaining_raw_data() -> None:
    reader = RegistryMetadataReader(FakeRegistry({"Token": "secret-save-value"}))

    snapshot = reader.snapshot((r"HKEY_CURRENT_USER\Software\Game",))
    encoded = json.dumps(asdict(snapshot), ensure_ascii=False)

    assert "secret-save-value" not in encoded
    assert snapshot.keys[0].values[0].sha256 == hashlib.sha256(
        b"REG_SZ\0secret-save-value"
    ).hexdigest()
```

- [x] **步骤 2：运行注册表测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_registry.py -v
```

预期：导入失败。

- [x] **步骤 3：扩展 WindowsRegistry 的只读枚举边界**

增加 `iter_metadata(key, *, max_subkey_depth=4, max_keys=256, max_values=2048)`；只打开已批准的 HKCU/HKLM 键。每个值按“注册表类型名称 + NUL + 确定性字节表示”计算 SHA-256，离开函数后不保留原始值。超过限制时返回 `truncated=True`。

- [x] **步骤 4：实现前后快照差异和候选**

只对创建、删除或摘要变化的值产生证据；候选路径始终是批准的注册表键，不推导未批准父键。注册表候选 `preselected=False`，接受时必须再次确认。

- [x] **步骤 5：运行注册表及存档打开回归**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_registry.py tests/unit/platform/windows/test_save_openers.py -v
git diff --check
```

预期：新快照测试和现有注册表打开测试均通过。

---

### 任务 6：实现可取消、可报告溢出的 Win32 目录观察器

**文件：**

- 新建：`src/gameshelf/platform/windows/directory_watcher.py`
- 新建：`tests/unit/platform/windows/test_directory_watcher.py`
- 新建：`tests/integration/saves/test_windows_directory_watcher.py`

**接口：**

- 产出：`WindowsDirectoryWatcher.start(root, sink) -> DirectoryWatchHandle`。
- 产出：`DirectoryWatchSink.on_change()`、`on_overflow()`、`on_failure()` 协议。
- 消费：任务 3 的 `RawFileChange`，不直接知道数据库或评分器。

- [x] **步骤 1：先写 WinAPI 缓冲解析和零字节溢出测试**

将 CreateFile、ReadDirectoryChanges、CancelIoEx、CloseHandle 包在可注入 `_DirectoryApi` 协议中。单元测试构造 `FILE_NOTIFY_INFORMATION` 字节，验证 created/modified/renamed-old/renamed-new 配对；Read 返回 `b""` 时只调用 `on_overflow(root)`。

- [x] **步骤 2：运行观察器单元测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/platform/windows/test_directory_watcher.py -v
```

预期：导入失败。

- [x] **步骤 3：实现窄 Win32 绑定**

使用 `ctypes.WinDLL("kernel32", use_last_error=True)` 绑定 `CreateFileW`、`ReadDirectoryChangesW`、`CancelIoEx` 和 `CloseHandle`。固定：

```python
BUFFER_SIZE = 64_000
NOTIFY_FILTER = (
    FILE_NOTIFY_CHANGE_FILE_NAME
    | FILE_NOTIFY_CHANGE_DIR_NAME
    | FILE_NOTIFY_CHANGE_SIZE
    | FILE_NOTIFY_CHANGE_LAST_WRITE
    | FILE_NOTIFY_CHANGE_CREATION
)
```

句柄访问为 `FILE_LIST_DIRECTORY`，共享读/写/删除，`bWatchSubtree=True`。不订阅 last-access 和 security 变化，减少噪声。

- [x] **步骤 4：实现线程、取消和故障传递**

每个根目录一个命名线程 `gameshelf-save-watch-<n>`。`DirectoryWatchHandle.stop()` 必须幂等：设置停止标志、调用 `CancelIoEx`、等待线程、关闭句柄。非取消 OSError 通过 `on_failure(root, code)` 报告，不能让线程静默死亡。

- [x] **步骤 5：增加真实 Windows 临时目录集成测试**

真实启动观察器后依次创建文件、修改文件、使用 `Path.replace()` 原子重命名；使用 `threading.Event.wait(timeout=3)` 条件等待，禁止固定长时间 sleep。测试结束在 `finally` 中停止句柄，断言没有对应命名线程存活。

- [x] **步骤 6：运行观察器单元与集成测试**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/platform/windows/test_directory_watcher.py tests/integration/saves/test_windows_directory_watcher.py -v
.\.venv\Scripts\python.exe -m ruff check src/gameshelf/platform/windows/directory_watcher.py tests/unit/platform/windows/test_directory_watcher.py tests/integration/saves/test_windows_directory_watcher.py
git diff --check
```

预期：事件、溢出、取消和线程清理测试通过。

---

### 任务 7：实现只控制生命周期的 Windows 进程树跟踪

**文件：**

- 新建：`src/gameshelf/platform/windows/process_tree.py`
- 新建：`tests/unit/platform/windows/test_process_tree.py`
- 新建：`tests/integration/saves/test_windows_process_tree.py`

**接口：**

- 产出：`WindowsProcessTreeTracker.start(root_pid, sink) -> ProcessTreeHandle`。
- 产出：`ProcessTreeSink.on_tree_exit()` 和 `on_tracking_degraded(reason)`。
- 不产出文件归因数据。

- [x] **步骤 1：先写根进程、子进程和首轮缺失测试**

使用注入快照序列：

```python
def test_tracker_waits_until_seen_child_exits() -> None:
    snapshots = iter([
        (ProcessRecord(10, 1), ProcessRecord(11, 10)),
        (ProcessRecord(11, 10),),
        (),
    ])
    sink = RecordingProcessSink()
    tracker = WindowsProcessTreeTracker(lambda: next(snapshots), poll_seconds=0)

    tracker.poll_once(10, sink)
    tracker.poll_once(10, sink)
    tracker.poll_once(10, sink)

    assert sink.exits == 1
    assert sink.degraded == []
```

根 PID 在第一次成功快照中已经消失且没有发现子进程时，调用 degraded 而不是 exit。

- [x] **步骤 2：运行进程树测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/platform/windows/test_process_tree.py -v
```

预期：导入失败。

- [x] **步骤 3：实现 Tool Help 快照和轮询句柄**

通过 `CreateToolhelp32Snapshot(TH32CS_SNAPPROCESS, 0)`、`Process32FirstW`、`Process32NextW` 读取 `(pid, parent_pid)`。跟踪器维护已经观察到的后代集合；只有根或任一已观察后代确实出现过，随后整个集合消失，才报告可靠退出。

轮询间隔默认 1 秒；句柄停止使用 `Event` 并等待命名线程退出。快照 API 失败一次立即降级并停止自动退出判断。

- [x] **步骤 4：增加真实 Python 子进程集成测试**

启动一个短生命周期 Python 父进程，父进程再启动子进程并先退出。断言跟踪器等待子进程结束后才报告树退出；测试进程命令只写临时目录，不接触项目数据。

- [x] **步骤 5：运行进程树测试**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/platform/windows/test_process_tree.py tests/integration/saves/test_windows_process_tree.py -v
git diff --check
```

预期：可靠退出、降级和清理均通过。

---

### 任务 8：实现单活动会话状态机和自动收尾

**文件：**

- 新建：`src/gameshelf/saves/guided_service.py`
- 新建：`tests/unit/saves/test_guided_service.py`
- 新建：`tests/integration/saves/test_guided_session.py`

**接口：**

- 消费：任务 1～7 的仓库、范围、聚合、补扫、注册表、评分、观察器、进程树和现有 `GameLauncher`。
- 产出：`preview()`、`start()`、`current()`、`status(session_id)`、`latest_for_game(game_id)`、`mark_saved()`、`stop_and_analyze()`、`cancel()`、`request_close()`、`resolve_close()`、`close()`。
- 产出：桥接层使用的稳定异常 `GuidedSaveError(code, message)`。

- [x] **步骤 1：先写状态机测试**

使用假的观察器、进程跟踪器、调度器和 launcher 覆盖：观察器全部就绪后才 launch；第二会话拒绝；current 优先返回活动会话、没有活动会话时返回最近未审核结果；status 只按会话 ID 读取；latest_for_game 不跨游戏；mark 保存一次并在 3 秒后分析；重复 mark 幂等；30 分钟到期分析；自动存档 stop；取消清理；启动失败清理；可靠进程退出收尾；跟踪降级不自动结束；close 请求状态。

```python
def test_mark_saved_schedules_one_three_second_settle(
    guided_service: GuidedServiceHarness,
) -> None:
    session = guided_service.start()

    first = guided_service.service.mark_saved(session.id)
    second = guided_service.service.mark_saved(session.id)

    assert first.status == second.status == "settling"
    assert guided_service.scheduler.delays == [3.0]
    guided_service.scheduler.run_next()
    assert guided_service.repository.get_session(session.id).status == "completed"
```

- [x] **步骤 2：运行状态机测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_service.py -v
```

预期：导入失败。

- [x] **步骤 3：实现线程安全运行态**

`GuidedSaveSessionService` 使用一个 `RLock` 和一个 `_ActiveRuntime`：

```python
@dataclass(slots=True)
class _ActiveRuntime:
    session_id: str
    game_id: str
    started_monotonic_ns: int
    save_mark_monotonic_ns: int | None
    aggregator: GuidedChangeAggregator
    watch_handles: list[DirectoryWatchHandle]
    process_handle: ProcessTreeHandle | None
    registry_before: RegistrySnapshot
    duration_timer: CancelHandle
    settle_timer: CancelHandle | None = None
    analysis_started: bool = False
```

构造函数使用专用 `ThreadPoolExecutor(max_workers=1, thread_name_prefix="gameshelf-guided-save")` 执行补扫和评分，不占用 `TaskRegistry`。

- [x] **步骤 4：实现开始顺序和失败回滚**

`start()`：重新解析选择范围 → 创建 preparing 记录 → 获取注册表基线 → 逐个启动观察器 → 调用 `GameLauncher.launch()` → 启动进程跟踪 → 更新 monitoring → 启动 30 分钟定时器。任一步失败按相反顺序关闭已创建资源，终态设为 failed，已有存档位置不变。

- [x] **步骤 5：实现保存标记、手动停止和分析**

`mark_saved()` 原子写入保存 UTC 时间和 monotonic 时间，状态进入 settling，只安排一次 3 秒定时器。`stop_and_analyze()` 无标记立即进入后台分析。分析顺序：停止观察器和进程句柄 → 获取注册表后快照 → 仅扫描 overflow/failed roots → 评分 → `repository.complete()` → 清空 `_ActiveRuntime`。

- [x] **步骤 6：实现取消、异常中断和应用关闭**

`cancel()` 在三个活动状态中都停止句柄、取消定时器、清空事件并写 cancelled。`close()` 在仍有活动运行态时停止资源并写 interrupted；正常 UI 应先通过停止分析或取消解决会话。应用启动装配时先调用 `recover_interrupted()`。

`request_close()` 的返回契约是：没有活动会话时返回 `True` 允许窗口关闭；有活动会话时只设置内存 `close_requested=True` 并返回 `False` 阻止窗口关闭。`resolve_close("return")` 清除请求，`resolve_close("cancel_and_exit")` 取消，`resolve_close("analyze_and_exit")` 收尾并在完成后调用注入的退出回调。

- [x] **步骤 7：增加端到端假的观察器集成测试**

测试创建临时游戏和存档目录，fake watcher 将实际变化送入聚合器；调用 start → 写文件 → mark → 触发 fake scheduler，断言 completed 候选和现有 save_locations 未自动改变。

- [x] **步骤 8：运行会话测试和相关启动回归**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_service.py tests/integration/saves/test_guided_session.py tests/unit/library/test_launcher.py -v
.\.venv\Scripts\python.exe -m ruff check src/gameshelf/saves/guided_service.py tests/unit/saves/test_guided_service.py tests/integration/saves/test_guided_session.py
git diff --check
```

预期：状态机、资源清理和现有启动器测试通过。

---

### 任务 9：实现事务式候选审核、共享位置写入和桥接 API

**文件：**

- 新建：`src/gameshelf/saves/location_persistence.py`
- 新建：`src/gameshelf/saves/guided_review.py`
- 修改：`src/gameshelf/saves/service.py`
- 修改：`src/gameshelf/bridge/api.py`
- 修改：`tests/integration/saves/test_save_location_service.py`
- 新建：`tests/integration/saves/test_guided_review.py`
- 新建：`tests/unit/bridge/test_guided_save_api.py`

**接口：**

- 产出：`PreparedSaveLocation` 和 `upsert_confirmed_location(connection, prepared)`。
- 产出：`GuidedSaveReviewService.accept(session_id, discovery_ids, confirm_registry)` 与 `discard(session_id)`。
- 产出：设计文档中的十二个桥接接口及 DTO。

- [x] **步骤 1：先写多候选整体回滚和幂等测试**

测试选中两个 discovery，第二个属于其他会话时整体失败且第一个没有进入 `save_locations`；合法重复接受返回同一 location ID；注册表未确认返回稳定错误。

- [x] **步骤 2：运行审核测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/integration/saves/test_guided_review.py -v
```

预期：导入失败。

- [x] **步骤 3：抽取共享连接内 upsert**

`location_persistence.py` 定义：

```python
@dataclass(frozen=True, slots=True)
class PreparedSaveLocation:
    game_id: str
    kind: SaveLocationKind
    path_template: str
    display_path: str
    path_key: str
    source: SaveLocationSource
    confidence: float
    evidence: tuple[str, ...]

def upsert_confirmed_location(
    connection: sqlite3.Connection,
    prepared: PreparedSaveLocation,
) -> SaveLocation: ...
```

把 `SaveLocationService.accept_suggestion()` 的验证拆成 `prepare_suggestion()`；原有手动和静态接受继续复用相同验证与 upsert，不改变公开行为。

- [x] **步骤 4：实现 `GuidedSaveReviewService` 单事务**

先用只读连接校验会话、候选、归属、状态和注册表确认，再准备全部位置。随后一次 `DbWriter.submit()` 内逐个 upsert，并把对应 discovery 更新为 accepted 和 `save_location_id`。任一候选无效使整个 writer 事务回滚。

- [x] **步骤 5：先写桥接契约失败测试**

覆盖空 selectedScopeIds、未知 scope、错误状态、重复 mark、注册表确认、候选失效、cancel、discard 和稳定错误码。DTO 使用 camelCase，且不暴露 `path_key` 或内部 Win32 错误文本。

- [x] **步骤 6：实现桥接方法和 DTO**

新增方法签名：

```python
def preview_guided_save_detection(self, request: object) -> ApiResult: ...
def start_guided_save_detection(self, request: object) -> ApiResult: ...
def current_guided_save_detection(self) -> ApiResult: ...
def guided_save_detection_status(self, request: object) -> ApiResult: ...
def latest_guided_save_detection_for_game(self, request: object) -> ApiResult: ...
def mark_guided_save_saved(self, request: object) -> ApiResult: ...
def stop_guided_save_detection(self, request: object) -> ApiResult: ...
def cancel_guided_save_detection(self, request: object) -> ApiResult: ...
def list_guided_save_discoveries(self, request: object) -> ApiResult: ...
def accept_guided_save_discoveries(self, request: object) -> ApiResult: ...
def discard_guided_save_detection(self, request: object) -> ApiResult: ...
def resolve_guided_close(self, request: object) -> ApiResult: ...
```

`start` 只接受 `gameId`、`selectedScopeIds: string[]` 和 `additionalDirectories: string[]`；后端重新构造和验证范围。`guided_save_detection_status` 接受 `sessionId`；`latest_guided_save_detection_for_game` 接受 `gameId`。`accept` 接受 `sessionId`、`discoveryIds`、`confirmRegistry`。

会话表只持久化 `game_id`，不重复保存可能变化的标题；桥接序列化会话 DTO 时通过现有 `LibraryService.get_game(session.game_id)` 填充 `gameTitle`。游戏已不存在时返回稳定的 `game_not_found` 错误，不向前端暴露仓库内部异常。

- [x] **步骤 7：运行审核、桥接和现有存档回归**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/integration/saves/test_guided_review.py tests/unit/bridge/test_guided_save_api.py tests/integration/saves/test_save_location_service.py tests/unit/bridge/test_save_location_api.py -v
.\.venv\Scripts\python.exe -m mypy src/gameshelf/saves/location_persistence.py src/gameshelf/saves/guided_review.py src/gameshelf/bridge/api.py
git diff --check
```

预期：动态审核事务、静态建议和手动位置回归通过。

---

### 任务 10：装配生命周期、窗口关闭协调并备份开发数据库

**文件：**

- 修改：`src/gameshelf/bootstrap/application.py`
- 修改：`src/gameshelf/app.py`
- 修改：`tests/integration/test_application_bootstrap.py`
- 新建：`tests/unit/test_app_close.py`
- 移动但不提交：`data/library.db`、`data/library.db-wal`、`data/library.db-shm`

**接口：**

- `Application` 新增 `guided_saves: GuidedSaveSessionService`。
- `Application.close()` 先关闭引导服务，再关闭通用任务、资源服务和 writer。
- pywebview `window.events.closing` 在活动会话存在时返回 `False`，由轮询状态触发前端关闭选择。

- [x] **步骤 1：先写装配和幂等关闭测试**

测试 `build_application()` 后 guided service 可预览；两次 `application.close()` 不报错；活动 fake 会话关闭时先收到 interrupted，再关闭 writer。

- [x] **步骤 2：先写窗口关闭拦截测试**

把关闭判断抽成可测函数：

```python
def _allow_window_close(application: Application) -> bool:
    if application.guided_saves.request_close():
        return True
    return False
```

无活动会话返回 True；有活动会话返回 False 且状态 DTO 的 `closeRequested=True`。

- [x] **步骤 3：运行装配测试并确认失败**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/integration/test_application_bootstrap.py tests/unit/test_app_close.py -v
```

预期：因 Application 尚无 guided service 或关闭协调而失败。

- [x] **步骤 4：在 composition root 创建并注入全部组件**

复用同一个 `KnownFolders`、`WindowsRegistry`、`PathTemplateResolver`、`GameLauncher` 和 `DbWriter`。创建顺序：仓库/范围/观察器/进程跟踪/注册表/补扫/评分 → session service → review service → BridgeApi。启动时在接受桥接请求前调用 `recover_interrupted()`。

- [x] **步骤 5：接入 pywebview closing 事件**

保留现有 `closed += application.close`，增加 `closing +=` 同步处理器。处理器只设置 closeRequested 并返回 False；不从 Python 主动执行 JavaScript。前端下一次 1 秒轮询显示关闭选择。

- [x] **步骤 6：安全备份当前开发数据库**

先确认 GameShelf 窗口和使用该仓库的 Python 进程已关闭。使用同一个 PowerShell 会话验证路径并移动三个精确文件：

```powershell
$workspaceRoot = (Resolve-Path -LiteralPath '.').Path
$dataRoot = (Resolve-Path -LiteralPath '.\data').Path
if (-not $dataRoot.StartsWith($workspaceRoot, [System.StringComparison]::OrdinalIgnoreCase)) {
  throw 'data 目录不在当前工作区内。'
}
$stamp = Get-Date -Format 'yyyyMMdd-HHmmss'
$backupRoot = Join-Path $dataRoot "db_backups\pre-guided-save-$stamp"
New-Item -ItemType Directory -Path $backupRoot | Out-Null
foreach ($name in @('library.db', 'library.db-wal', 'library.db-shm')) {
  $source = Join-Path $dataRoot $name
  if (Test-Path -LiteralPath $source) {
    Move-Item -LiteralPath $source -Destination (Join-Path $backupRoot $name)
  }
}
Get-ChildItem -LiteralPath $backupRoot | Select-Object Name, Length
```

如果任一 move 因文件占用失败，停止本步骤并关闭持有数据库的程序；不删除、不强制覆盖。备份目录属于本地 `data`，不得加入 Git。

- [x] **步骤 7：运行源码冒烟并确认新数据库**

运行：

```powershell
.\.venv\Scripts\python.exe -m gameshelf --smoke-test
.\.venv\Scripts\python.exe -c "import sqlite3; c=sqlite3.connect('data/library.db'); print(c.execute('PRAGMA user_version').fetchone()[0]); print(c.execute('PRAGMA table_info(save_detection_sessions)').fetchall()); c.close()"
```

预期：冒烟输出 schema 1，新 `data/library.db` 包含最终字段，时间戳备份仍存在。

- [x] **步骤 8：运行装配回归和审核检查点**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/integration/test_application_bootstrap.py tests/unit/test_app_close.py -v
git diff --check
git status --short
```

预期：应用关闭两次仍安全，README 未进入任何暂存区，数据库备份不出现在 Git 状态中。

---

### 任务 11：增加前端契约、全局会话 store、状态栏和受控详情恢复

**文件：**

- 修改：`frontend/src/api/contracts.ts`
- 修改：`frontend/src/api/mockBridge.ts`
- 新建：`frontend/src/features/saves/guidedSaveStore.ts`
- 新建：`frontend/src/features/saves/GuidedSaveStatusBar.vue`
- 新建：`frontend/src/features/saves/GuidedSaveCloseDialog.vue`
- 修改：`frontend/src/App.vue`
- 修改：`frontend/src/features/library/GameGrid.vue`
- 新建：`frontend/tests/guidedSaveStore.spec.ts`
- 新建：`frontend/tests/GuidedSaveStatusBar.spec.ts`
- 修改：`frontend/tests/App.spec.ts`
- 修改：`frontend/tests/GameGrid.spec.ts`

**接口：**

- 产出：TypeScript `GuidedSavePreview`、`GuidedSaveSession`、`GuidedSaveDiscovery` 和桥接方法。
- 产出：Pinia `useGuidedSaveStore`，全局唯一轮询计时器。
- `GameGrid` 改为 `v-model:selected-game-id`，状态栏可恢复正确游戏详情。

- [x] **步骤 1：先写前端类型和 store 轮询测试**

测试 active API 返回 null 时不安排计时器；活动状态每 1000ms 轮询；completed/cancelled/failed/interrupted 后停止；组件卸载不重复创建 timer；恢复按钮设置目标游戏 ID。

- [x] **步骤 2：运行 store 测试并确认失败**

运行：

```powershell
npm --prefix frontend run test:unit -- --run tests/guidedSaveStore.spec.ts
```

预期：模块不存在。

- [x] **步骤 3：定义前端 DTO**

`contracts.ts` 使用以下核心结构：

```ts
export type GuidedSessionStatus =
  | 'preparing' | 'monitoring' | 'settling' | 'completed'
  | 'cancelled' | 'failed' | 'interrupted'

export type GuidedSaveSession = {
  id: string
  gameId: string
  gameTitle: string
  status: GuidedSessionStatus
  startedAt: string
  monitoringStartedAt: string | null
  saveMarkedAt: string | null
  finishedAt: string | null
  changeCount: number
  processTrackingDegraded: boolean
  overflowedScopes: string[]
  truncatedScopes: string[]
  closeRequested: boolean
  error: { code: string; message: string } | null
}
```

为任务 9 的十二个桥接方法加入精确请求和返回类型；mockBridge 提供无活动会话和空候选的安全默认值。

- [x] **步骤 4：实现全局 store**

store state 为 `session`、`discoveries`、`error`、`pollTimer`、`requestedGameId`。启动时调用 `current_guided_save_detection`；进入详情时调用 `latest_guided_save_detection_for_game`；`startPolling(sessionId)` 先清理旧 timer，再立即调用 `guided_save_detection_status`，仅在 preparing/monitoring/settling 时安排 1000ms timeout。所有命令成功后立即刷新，不等待下一个 tick。

- [x] **步骤 5：把 GameGrid 详情选择改为受控 ID**

新增 prop `selectedGameId: string | null` 和 emit `update:selectedGameId`。内部 `selected` 改为 computed；卡片打开 emit ID，关闭 emit null。App 使用 `store.selectedGameId`，状态栏恢复操作设置为活动 `gameId`。批量管理开始时清空 ID，保留现有焦点恢复测试。

- [x] **步骤 6：实现状态栏和关闭选择**

`GuidedSaveStatusBar` 仅在活动状态显示游戏名、状态、持续时间和“返回向导”。`GuidedSaveCloseDialog` 仅在 `closeRequested` 时显示三个按钮：停止并分析后退出、取消会话并退出、返回程序；分别调用 store 的 `resolveClose()`。

- [x] **步骤 7：接入 App 启动和布局**

bootstrap 完成 library load 后调用 `guidedStore.refreshActive(bridge)`；App 根部渲染状态栏和关闭对话框。详情关闭不停止 store 轮询。状态栏不展示原始文件路径。

- [x] **步骤 8：运行前端 store、App、GameGrid 测试和类型检查**

运行：

```powershell
npm --prefix frontend run test:unit -- --run tests/guidedSaveStore.spec.ts tests/GuidedSaveStatusBar.spec.ts tests/App.spec.ts tests/GameGrid.spec.ts
npm --prefix frontend run type-check
git diff --check
```

预期：轮询、恢复详情、批量模式和现有 App 测试通过。

---

### 任务 12：实现范围确认、监控状态和候选审核界面

**文件：**

- 新建：`frontend/src/features/saves/GuidedSavePanel.vue`
- 新建：`frontend/src/features/saves/GuidedSaveDiscoveries.vue`
- 修改：`frontend/src/features/saves/SaveLocationList.vue`
- 修改：`frontend/src/features/library/library.css`
- 新建：`frontend/tests/GuidedSavePanel.spec.ts`
- 新建：`frontend/tests/GuidedSaveDiscoveries.spec.ts`
- 修改：`frontend/tests/SaveLocationList.spec.ts`

**接口：**

- 消费：任务 11 的 store 和 DTO。
- `GuidedSavePanel` emit `accepted`，由 `SaveLocationList` 刷新正式位置。
- 静态 `SaveSuggestionList` 保持独立状态和按需按钮。

- [x] **步骤 1：先写范围确认和监控状态组件测试**

覆盖：未开始显示独立按钮；点击后只调用 preview，不启动；默认复选；ProgramData 不选；不可用项禁用；确认传 selected IDs；preparing 文案说明游戏未启动；monitoring 显示“我刚刚保存了”“停止并分析”“取消会话”；settling 禁止重复命令；其他游戏活动时按钮禁用并说明游戏名。

- [x] **步骤 2：运行 Panel 测试并确认失败**

运行：

```powershell
npm --prefix frontend run test:unit -- --run tests/GuidedSavePanel.spec.ts
```

预期：组件不存在。

- [x] **步骤 3：实现范围确认面板**

点击“引导式寻找”才调用 preview。面板显示启动程序、范围复选、定向注册表键、不可用原因和隐私说明。额外目录通过既有 `choose_directory()` 选择，随后由 start 后端重新验证；选择结果在列表中显示但不直接访问本地文件。

- [x] **步骤 4：实现会话命令和 3 秒反馈**

monitoring 主按钮调用 `mark_guided_save_saved`；成功后立即显示 settling 的“正在等待游戏完成写盘，约 3 秒”。自动存档按钮调用 stop；取消先用中文确认框说明不产生候选且不删除文件。

- [x] **步骤 5：先写候选审核测试**

覆盖高/中/低可信和注册表分组；高可信正常文件系统候选默认勾选；无标记、overflow/truncated 或 registry 不预选；展开显示代表文件和证据；注册表接受二次确认；接受 emit；丢弃确认；空结果引导手动添加和静态查找。

- [x] **步骤 6：实现 `GuidedSaveDiscoveries`**

复用现有存档建议卡片视觉语言，但使用独立标题“引导式寻找结果”。低可信组默认 `<details>` 折叠。接受请求只发送 discovery IDs 和 `confirmRegistry`，成功后清空已接受项并 emit accepted。

- [x] **步骤 7：接入 SaveLocationList 和样式**

在 `SaveSuggestionList` 的静态区域旁加入 `GuidedSavePanel`，保持“查找存档”原按钮与提示不变。新增状态栏、向导、范围列表和候选警告样式；80%～120% UI scale 和窄窗口下不产生水平页面溢出。

- [x] **步骤 8：运行存档界面测试、全前端测试和构建**

运行：

```powershell
npm --prefix frontend run test:unit -- --run tests/GuidedSavePanel.spec.ts tests/GuidedSaveDiscoveries.spec.ts tests/SaveLocationList.spec.ts tests/SaveSuggestionList.spec.ts
npm --prefix frontend run test:unit -- --run
npm --prefix frontend run type-check
npm --prefix frontend run build
git diff --check
```

预期：引导式与静态建议互不自动触发，全部前端测试、类型检查和生产构建通过。

---

### 任务 13：完整集成、压力边界、文档状态与最终验证

**文件：**

- 新建：`tests/integration/saves/test_guided_save_end_to_end.py`
- 新建：`tests/unit/saves/test_guided_limits.py`
- 修改：`docs/superpowers/plans/2026-08-12-GameShelf-06-动态与孤立存档发现.md`
- 修改：`docs/superpowers/plans/2026-08-12-GameShelf-开发路线图.md`
- 修改：`docs/历史归档/2026-08/2026-08-15-引导式寻找存档实施计划.md`

**接口：**

- 不再新增产品接口；本任务证明全链路满足固定设计。

- [x] **步骤 1：增加普通保存端到端测试**

使用临时游戏目录、fake launcher、真实或可控观察器适配层：preview → start → 写入 `slot1.sav` → mark → 触发 3 秒收尾 → list discoveries → accept。断言最终 `save_locations.source == "dynamic"`，模板可展开，原文件字节完全未变。

- [x] **步骤 2：增加自动存档、噪声和原子替换测试**

无保存标记时调用 stop；同时写浏览器缓存式路径和真实 save 目录；使用 temp 文件 replace。断言真实目录排名更高、缓存过滤、无标记不预选、原子替换只产生最终候选。

- [x] **步骤 3：增加失败与恢复端到端测试**

覆盖：观察器第二个根启动失败时游戏未启动；launcher 失败关闭全部观察器；根目录中途失效显示 incomplete；overflow 只扫描受影响根；取消不生成 discovery；残留 monitoring 会话重启变 interrupted；已有 save_locations 不变。

- [x] **步骤 4：增加硬上限压力测试**

使用纯内存事件生成器记录 50,001 个事件和 20,001 个不同路径，断言容器长度不超过设计上限、dropped 计数正确、排序确定。补扫器使用缩小 deadline 的注入时钟证明时间限制，而不是让 CI 实际等待 5 秒。

- [x] **步骤 5：运行新增完整测试集**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/saves/test_guided_limits.py tests/integration/saves/test_guided_save_end_to_end.py -v
```

预期：普通保存、自动保存、噪声、失败、恢复和上限测试通过。

- [x] **步骤 6：运行 Python 完整质量门禁**

运行：

```powershell
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m ruff check src tests scripts
.\.venv\Scripts\python.exe -m mypy src scripts/update_ludusavi_snapshot.py scripts/benchmark_ludusavi_index.py
.\.venv\Scripts\python.exe -m gameshelf --smoke-test
```

预期：全量测试零失败，Ruff 和 mypy 零问题，冒烟输出 `GameShelf bootstrap OK (schema 1)`。

实际：Python 443 项全部通过；Ruff、mypy（88 个源码文件）和 schema 1 源码冒烟通过。最终验证前修复了第一次取消恰逢下一次读取尚未挂起时的观察线程停止竞态，并增加确定性回归测试。

- [x] **步骤 7：运行前端完整质量门禁**

运行：

```powershell
npm --prefix frontend run test:unit -- --run
npm --prefix frontend run type-check
npm --prefix frontend run build
```

预期：全部前端测试、类型检查和生产构建通过；仅构建产物发生预期变化时才更新 `resources/ui`，否则不得留下意外产物差异。

实际：前端 25 个测试文件、86 项测试全部通过；`vue-tsc` 类型检查和 Vite 生产构建通过，`resources/ui` 已更新为本次功能产物。

- [ ] **步骤 8：执行真实 Windows 人工验收**

至少验证：

1. 普通游戏保存后点击标记，约 3 秒后显示正确目录。
2. 自动存档游戏使用“停止并分析”。
3. 关闭详情后主页状态栏持续，可恢复向导。
4. 第二个游戏不能并发开始。
5. 空闲监控时任务管理器 CPU 接近空闲，磁盘不出现周期性 AppData 全量读取。
6. 启动器脱离时出现降级提示，不自动误判退出。
7. 取消、关闭程序和重复开始后无残留 `gameshelf-save-watch-*` 线程。
8. 80%、100%、120% 缩放和窄窗口下范围与候选界面可用。

- [x] **步骤 9：更新固定文档和计划勾选状态**

自动验证完成后更新实现状态；人工验收未完成时必须在固定文档和路线图中明确保留：

- 把模块 06 状态改为“V0.1 引导式寻找存档已实现、真实游戏验收待完成”。
- 在模块 06 变更记录写入实现日期和最终自动验证数量；没有真实资源观察结果时不得编造。
- 把路线图模块 06 标为功能已实现、待真实游戏验收，并将下一项改为模块 07 便携打包与发布。
- 勾选本实施计划所有已完成步骤；任何未完成的人工验收保持未勾选并如实说明。

- [x] **步骤 10：最终差异审核，不提交**

运行：

```powershell
git diff --check
git status --short
git diff --stat
git diff -- README.md
git diff --cached --name-only
```

预期：格式检查通过；README 仍只有审核测试注释；暂存区为空；所有产品、测试和中文文档修改保留为未提交状态，供 Codex 审核/撤销界面检查。
