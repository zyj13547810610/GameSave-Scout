# GameSave Scout 标题与版本号分离实施计划

> **供智能代理执行：** 必须使用 `superpowers:executing-plans` 按任务顺序实施；本项目明确不使用子代理。步骤使用复选框跟踪。

> **实施状态（2026-08-18）：** 任务 1～7 已按测试驱动顺序完成并分批提交。受影响区域回归为 Python 104 项、前端 5 个文件共 23 项；最终全量门禁为 Python 651 项（175.51 秒）和前端 32 个文件共 124 项（60.76 秒），Ruff、mypy（101 个源文件）、Vue 类型检查、85 模块生产构建及 `GameSave Scout bootstrap OK (schema 2)` 源码冒烟均通过。下方 6 项人工验收也已全部完成；期间发现并修复 VNDB 批量搜索中切换游戏后任务轮询停留的问题。本轮由用户主动删除可舍弃的开发数据并重建数据库，未重建便携包、未执行 `git push`。

**目标：** 把游戏标题与版本号建模为两个独立字段；保留带版本号的本地展示和检索能力，同时确保 VNDB 只使用不含版本号的标题查询。

**架构：** SQLite schema 升级到 2，在 `games` 表加入版本号、自动检测版本号和手动保护标记；扫描层用保守的纯函数拆分目录名，重扫协调器分别维护检测值和用户值；桥接层以一次原子操作保存标题与版本号；游戏库和批量封面向导分别展示两个字段，VNDB 客户端仍只消费 `Game.title`。

**技术栈：** Python 3.12、SQLite、pytest、Ruff、mypy、pywebview、Vue 3、TypeScript、Pinia、Vitest、Vite。

## 全局约束

- 直接在当前工作区实施，不创建分支、worktree 或子代理。
- 仓库内设计和开发文档使用中文；固定设计只更新 `docs/superpowers/plans` 中现有对应文档，本文件作为一次性详细实施计划保存在 `docs/历史归档`。
- 不添加 Python 或前端运行时依赖。
- 数据库升级只增加字段，不拆分或重写 schema 1 中已有游戏标题；用户会自行删除可舍弃的本地数据库并重新扫描。
- 不自动删除 `data/library.db`、封面或其他用户数据。
- 自动拆分必须保守：只识别标题末尾带明确 `v`、`ver`、`version`、`build` 标记的版本号；裸数字、年份、`DLC`、`PC`、`x64`、`Final` 一律不拆。
- 保存标题与版本号必须在同一条数据库更新和同一事务中完成；手动清空版本号也必须被记为手动决定，后续重扫不得恢复自动值。
- VNDB 当前游戏搜索和全部游戏搜索只发送 `Game.title`；版本号仅用于本地展示、筛选和进度文字。
- 每个任务遵循测试驱动开发：先写会失败的测试并看到预期失败，再写最小实现，最后运行精确测试和静态检查。
- 每个任务通过后按计划提交；不把 `data`、`dist`、`.venv`、依赖目录或用户本地图片加入 Git。

---

## 文件结构映射

### Python 新文件

- `src/gamesave_scout/db/migrations/0002_initial.sql`：schema 1 到 schema 2 的追加字段迁移。
- `src/gamesave_scout/library/title_parser.py`：保守拆分标题和版本号的纯函数。
- `tests/unit/library/test_title_parser.py`：明确可拆与不可拆的标题矩阵。

### Python 修改文件

- `src/gamesave_scout/db/migrator.py`
- `src/gamesave_scout/library/models.py`
- `src/gamesave_scout/library/repository.py`
- `src/gamesave_scout/library/service.py`
- `src/gamesave_scout/scanning/service.py`
- `src/gamesave_scout/scanning/reconcile.py`
- `src/gamesave_scout/bridge/api.py`
- `src/gamesave_scout/covers/candidates.py`
- `src/gamesave_scout/covers/wizard_service.py`
- `src/gamesave_scout/app.py`
- `scripts/release_tools.py`

### Python 测试修改文件

- `tests/unit/db/test_migrator.py`
- `tests/unit/library/test_service.py`
- `tests/unit/bridge/test_library_api.py`
- `tests/integration/scanning/test_scan_service.py`
- `tests/integration/covers/test_cover_wizard_service.py`
- `tests/unit/bridge/test_cover_wizard_api.py`
- `tests/integration/test_application_bootstrap.py`
- `tests/integration/test_pyinstaller_spec.py`
- `tests/unit/scripts/test_release_tools.py`

### 前端修改文件

- `frontend/src/api/contracts.ts`
- `frontend/src/api/mockBridge.ts`
- `frontend/src/features/library/GameSettingsPanel.vue`
- `frontend/src/features/library/GameCard.vue`
- `frontend/src/features/library/GameDetailDrawer.vue`
- `frontend/src/features/library/libraryFilters.ts`
- `frontend/src/features/library/library.css`
- `frontend/src/features/covers/CoverWizardQueue.vue`
- `frontend/src/features/covers/CoverWizardWorkspace.vue`
- `frontend/src/features/covers/CoverCandidateToolbar.vue`

### 前端测试修改文件

- `frontend/tests/GameSettingsPanel.spec.ts`
- `frontend/tests/GameGrid.spec.ts`
- `frontend/tests/libraryFilters.spec.ts`
- `frontend/tests/CoverWizardQueue.spec.ts`
- `frontend/tests/CoverWizardWorkspace.spec.ts`
- `frontend/tests/coverWizardStore.spec.ts`
- `frontend/tests/App.spec.ts`

### 固定文档与生成资源

- `docs/superpowers/plans/2026-08-12-GameSave-Scout-01-基础架构.md`
- `docs/superpowers/plans/2026-08-12-GameSave-Scout-02-游戏库扫描与启动.md`
- `docs/superpowers/plans/2026-08-12-GameSave-Scout-03-封面与游戏库界面.md`
- `docs/superpowers/plans/2026-08-12-GameSave-Scout-07-便携版打包与发布.md`
- `docs/superpowers/plans/2026-08-12-GameSave-Scout-开发路线图.md`
- `resources/ui/index.html`
- `resources/ui/assets/*`

---

### 任务 1：保守标题解析器

**文件：**

- 新建：`src/gamesave_scout/library/title_parser.py`
- 新建：`tests/unit/library/test_title_parser.py`

**接口：**

- 产出：`split_title_and_version(raw_name: str) -> tuple[str, str | None]`
- 输入输出均为纯文本，不访问数据库、文件系统或网络。

- [x] **步骤 1：写明确后缀拆分失败测试**

使用参数化测试覆盖下列精确结果：

```python
@pytest.mark.parametrize(
    ("raw_name", "expected"),
    [
        ("AoiChan.v1.0.8", ("AoiChan", "v1.0.8")),
        ("Game Name - Ver 1.42", ("Game Name", "Ver 1.42")),
        ("作品名（version 2.0）", ("作品名", "version 2.0")),
        ("Title_build_1234", ("Title", "build_1234")),
        ("アメリア Ver1.2", ("アメリア", "Ver1.2")),
    ],
)
def test_splits_only_explicit_version_suffixes(
    raw_name: str,
    expected: tuple[str, str | None],
) -> None:
    assert split_title_and_version(raw_name) == expected
```

- [x] **步骤 2：写保守不拆分失败测试**

```python
@pytest.mark.parametrize(
    "raw_name",
    [
        "Need for Speed 21 Heat",
        "Resident Evil 4",
        "Game 1.05",
        "Game DLC",
        "Game PC",
        "Game x64",
        "Final",
    ],
)
def test_keeps_ambiguous_suffixes_in_title(raw_name: str) -> None:
    assert split_title_and_version(raw_name) == (raw_name, None)
```

另测输入首尾空白被清理、只有版本标记而没有标题时返回整个清理后文本且版本为 `None`。

- [x] **步骤 3：运行解析器测试并确认模块不存在而失败**

运行：

```powershell
.venv\python.exe -m pytest tests/unit/library/test_title_parser.py -q
```

预期：测试收集失败，提示 `gamesave_scout.library.title_parser` 不存在。

- [x] **步骤 4：实现最小保守解析器**

```python
from __future__ import annotations

import re

_VERSION_TOKEN = r"(?:v(?:er(?:sion)?)?|build)[\s._-]*\d+(?:\.\d+)*"
_VERSION_SUFFIX = re.compile(
    rf"""^(?P<title>.+?)(?:
        [\s._-]+(?P<plain>{_VERSION_TOKEN})
        |
        \s*[（(]\s*(?P<bracket>{_VERSION_TOKEN})\s*[）)]\s*
    )$""",
    re.IGNORECASE | re.VERBOSE,
)


def split_title_and_version(raw_name: str) -> tuple[str, str | None]:
    value = raw_name.strip()
    match = _VERSION_SUFFIX.fullmatch(value)
    if match is None:
        return value, None
    title = match.group("title").strip()
    version = (match.group("plain") or match.group("bracket")).strip()
    if not title:
        return value, None
    return title, version
```

不得修改版本号中的大小写、点号、下划线或内部空格。

- [x] **步骤 5：运行任务 1 门禁**

```powershell
.venv\python.exe -m pytest tests/unit/library/test_title_parser.py -q
.venv\python.exe -m ruff check src/gamesave_scout/library/title_parser.py tests/unit/library/test_title_parser.py
.venv\python.exe -m mypy src/gamesave_scout/library/title_parser.py
git diff --check
```

预期：全部通过。

- [x] **步骤 6：提交解析器**

```powershell
git add src/gamesave_scout/library/title_parser.py tests/unit/library/test_title_parser.py
git commit -m "feat: 添加标题版本号保守解析器"
```

---

### 任务 2：数据库 schema 2 与发布载荷

**文件：**

- 新建：`src/gamesave_scout/db/migrations/0002_initial.sql`
- 修改：`src/gamesave_scout/db/migrator.py`
- 修改：`src/gamesave_scout/library/models.py`
- 修改：`src/gamesave_scout/library/repository.py`
- 修改：`src/gamesave_scout/app.py`
- 修改：`scripts/release_tools.py`
- 修改：`tests/unit/db/test_migrator.py`
- 修改：`tests/integration/test_application_bootstrap.py`
- 修改：`tests/integration/test_pyinstaller_spec.py`
- 修改：`tests/unit/scripts/test_release_tools.py`

**接口与 schema：**

- `LATEST_SCHEMA_VERSION = 2`
- `Game.version: str | None`
- `Game.detected_version: str | None`
- 迁移文件名继续服从现有 `Migrator.migration_sql()` 的命名约定，因此使用 `0002_initial.sql`。

- [x] **步骤 1：写 schema 2 迁移失败测试**

在 `tests/unit/db/test_migrator.py` 增加两个测试：

1. 空数据库迁移后 `PRAGMA user_version` 为 2，`games` 表存在 `version`、`detected_version`、`version_is_manual`。
2. 先手工执行 `Migrator.migration_sql(1)`，插入标题 `AoiChan.v1.0.8`，再执行 `migrate()`；断言旧标题不变、两个版本字段为 `NULL`、`version_is_manual = 0`，并产生升级前备份。

```python
assert result == 2
assert row["title"] == "AoiChan.v1.0.8"
assert row["version"] is None
assert row["detected_version"] is None
assert row["version_is_manual"] == 0
```

- [x] **步骤 2：运行迁移测试并确认版本断言失败**

```powershell
.venv\python.exe -m pytest tests/unit/db/test_migrator.py -q
```

预期：新增测试因最新版本仍为 1 或迁移文件不存在而失败。

- [x] **步骤 3：实现 schema 2 迁移**

`src/gamesave_scout/db/migrations/0002_initial.sql` 内容固定为：

```sql
ALTER TABLE games ADD COLUMN version TEXT;
ALTER TABLE games ADD COLUMN detected_version TEXT;
ALTER TABLE games ADD COLUMN version_is_manual INTEGER NOT NULL DEFAULT 0
    CHECK (version_is_manual IN (0, 1));

PRAGMA user_version = 2;
```

把 `LATEST_SCHEMA_VERSION` 改为 2；不得在迁移 SQL 中更新现有 `title` 或 `detected_title`。

- [x] **步骤 4：扩展领域模型和行映射**

在 `Game` 数据类末尾加入带默认值的字段，避免破坏已有测试中的关键字构造：

```python
version: str | None = None
detected_version: str | None = None
```

`game_from_row()` 从 SQLite 行读取两个字段。领域模型暂不暴露 `title_is_manual` 和 `version_is_manual`，手动保护仍由写入服务和协调 SQL 管理。

- [x] **步骤 5：写并实现当前 schema 的启动和打包断言**

- `src/gamesave_scout/app.py::_smoke_report` 使用 `LATEST_SCHEMA_VERSION`，不再硬编码 1。
- `tests/integration/test_application_bootstrap.py` 对真实启动结果断言 `schemaVersion == 2`。
- `scripts/release_tools.py` 的关键文件清单加入 `_internal/gamesave_scout/db/migrations/0002_initial.sql`，发布元数据中的当前数据库版本改为 2。
- `tests/integration/test_pyinstaller_spec.py` 同时断言 spec 收集 `0001_initial.sql` 和 `0002_initial.sql`。
- `tests/unit/scripts/test_release_tools.py::_minimal_release_tree` 创建两个迁移文件，并让当前版本发布夹具使用 `database_schema_version=2`。

`GameSaveScout.spec` 已按 `*.sql` 收集迁移文件，只通过集成测试验证，不增加重复的单文件配置。

- [x] **步骤 6：运行任务 2 门禁**

```powershell
.venv\python.exe -m pytest tests/unit/db/test_migrator.py tests/integration/test_application_bootstrap.py tests/integration/test_pyinstaller_spec.py tests/unit/scripts/test_release_tools.py -q
.venv\python.exe -m ruff check src/gamesave_scout/db src/gamesave_scout/library/models.py src/gamesave_scout/library/repository.py src/gamesave_scout/app.py scripts/release_tools.py tests/unit/db/test_migrator.py tests/integration/test_application_bootstrap.py tests/integration/test_pyinstaller_spec.py tests/unit/scripts/test_release_tools.py
.venv\python.exe -m mypy src scripts
git diff --check
```

预期：全部通过；schema 1 升级测试证明旧标题未被隐式迁移。

- [x] **步骤 7：提交 schema 2**

```powershell
git add src/gamesave_scout/db/migrations/0002_initial.sql src/gamesave_scout/db/migrator.py src/gamesave_scout/library/models.py src/gamesave_scout/library/repository.py src/gamesave_scout/app.py scripts/release_tools.py tests/unit/db/test_migrator.py tests/integration/test_application_bootstrap.py tests/integration/test_pyinstaller_spec.py tests/unit/scripts/test_release_tools.py
git commit -m "feat: 升级游戏版本号数据库结构"
```

---

### 任务 3：原子保存标题与版本号

**文件：**

- 修改：`src/gamesave_scout/library/service.py`
- 修改：`src/gamesave_scout/bridge/api.py`
- 修改：`tests/unit/library/test_service.py`
- 修改：`tests/unit/bridge/test_library_api.py`

**接口：**

- 用 `LibraryService.set_game_metadata(game_id: str, title: str, version: str | None) -> Game` 取代 `set_game_title`。
- 用桥接 `set_game_metadata({ gameId, title, version }) -> ApiResult[Game]` 取代 `set_game_title`。
- 游戏 DTO 新增 `version: string | null`。

- [x] **步骤 1：写服务层原子保存失败测试**

覆盖：

- 标题和版本首尾空白被清理。
- `None` 或纯空白版本保存为 `NULL`。
- 保存后 `title_is_manual = 1` 且 `version_is_manual = 1`。
- 手动清空版本同样设置 `version_is_manual = 1`。
- 空标题抛出 `InvalidGameConfiguration`，标题和版本均保持原值。

```python
updated = service.set_game_metadata(game.id, "  自定义标题  ", "  Ver 2.0  ")

assert updated.title == "自定义标题"
assert updated.version == "Ver 2.0"
assert flags == (1, 1)
```

- [x] **步骤 2：运行服务测试并确认接口不存在而失败**

```powershell
.venv\python.exe -m pytest tests/unit/library/test_service.py -q
```

- [x] **步骤 3：实现同语句同事务更新**

```python
def set_game_metadata(
    self,
    game_id: str,
    title: str,
    version: str | None,
) -> Game:
    clean_title = title.strip()
    clean_version = version.strip() if version is not None else ""
    if not clean_title:
        raise InvalidGameConfiguration("游戏标题不能为空")
    return self._update_game(
        game_id,
        "title = ?, title_is_manual = 1, "
        "version = ?, version_is_manual = 1, updated_at = ?",
        (clean_title, clean_version or None, _utc_now()),
    )
```

不得先保存标题再保存版本；数据库写入失败时两者一起回滚。

- [x] **步骤 4：写桥接契约失败测试**

断言：

- `{gameId, title, version}` 成功并返回含 `version` 的游戏 DTO。
- `version: null` 合法。
- 缺少 `version`、版本为非字符串非 `null`、存在额外键时返回 `invalid_request`。
- 空标题返回现有稳定的无效游戏配置错误码。

- [x] **步骤 5：实现桥接与 DTO**

```python
def set_game_metadata(self, request: object) -> ApiResult:
    try:
        payload = _payload(request)
        _only_keys(payload, {"gameId", "title", "version"})
        if "version" not in payload:
            raise InvalidRequest("version is required.")
        version = payload["version"]
        if version is not None and not isinstance(version, str):
            raise InvalidRequest("version must be a string or null.")
        game = self._require_library().set_game_metadata(
            _string(payload, "gameId"),
            _string(payload, "title"),
            version,
        )
        return success(self._game_dto(game))
    except (InvalidRequest, InvalidGameConfiguration) as error:
        return failure("invalid_request", str(error))
    except GameNotFoundError:
        return failure("game_not_found", "没有找到对应的游戏。")
```

删除生产代码和测试对 `set_game_title` 的调用，不保留双写兼容接口。

- [x] **步骤 6：运行任务 3 门禁**

```powershell
.venv\python.exe -m pytest tests/unit/library/test_service.py tests/unit/bridge/test_library_api.py -q
.venv\python.exe -m ruff check src/gamesave_scout/library/service.py src/gamesave_scout/bridge/api.py tests/unit/library/test_service.py tests/unit/bridge/test_library_api.py
.venv\python.exe -m mypy src
git diff --check
```

- [x] **步骤 7：提交原子元数据接口**

```powershell
git add src/gamesave_scout/library/service.py src/gamesave_scout/bridge/api.py tests/unit/library/test_service.py tests/unit/bridge/test_library_api.py
git commit -m "feat: 原子保存游戏标题与版本号"
```

---

### 任务 4：扫描拆分、重扫保护与移动确认

**文件：**

- 修改：`src/gamesave_scout/scanning/service.py`
- 修改：`src/gamesave_scout/scanning/reconcile.py`
- 修改：`tests/integration/scanning/test_scan_service.py`

**接口与不变量：**

- 扫描候选载荷新增 `version: str | None`。
- 新游戏的用户字段和检测字段都来自解析器。
- 同路径重扫始终刷新 `detected_title`、`detected_version`；只在对应 `*_is_manual = 0` 时刷新用户字段。
- 确认移动继续保留旧游戏 ID 和用户标题、版本号，只刷新路径、文件身份和检测字段。

- [x] **步骤 1：写新扫描拆分失败测试**

创建目录 `AoiChan.v1.0.8` 和 `Need for Speed 21 Heat` 后扫描，断言：

```python
assert explicit.title == "AoiChan"
assert explicit.detected_title == "AoiChan"
assert explicit.version == "v1.0.8"
assert explicit.detected_version == "v1.0.8"

assert ambiguous.title == "Need for Speed 21 Heat"
assert ambiguous.version is None
```

- [x] **步骤 2：写重扫手动保护失败测试**

先扫描 `AoiChan.v1.0.8`，再调用生产接口：

```python
library.set_game_metadata(game.id, "自定义标题", None)
```

对相同目录重扫后断言用户标题仍为 `自定义标题`、用户版本仍为 `None`，而检测值恢复为 `AoiChan` 和 `v1.0.8`。这项测试专门证明“手动清空版本号”不会被自动检测覆盖。

- [x] **步骤 3：运行扫描测试并确认版本字段断言失败**

```powershell
.venv\python.exe -m pytest tests/integration/scanning/test_scan_service.py -q
```

- [x] **步骤 4：在扫描服务中只解析一次目录名**

```python
detected_title, detected_version = split_title_and_version(candidate.path.name)
payload = {
    "relativeDir": candidate.relative_dir,
    "title": detected_title,
    "version": detected_version,
    # 保留已有身份、可执行文件和状态字段
}
```

解析器只消费目录末级名称；不得把安装路径、可执行文件名或手工标题作为自动拆分输入。

- [x] **步骤 5：实现新建和重扫 SQL**

新建游戏时写入：

```sql
title = :title,
detected_title = :title,
version = :version,
detected_version = :version,
title_is_manual = 0,
version_is_manual = 0
```

已有同路径游戏更新使用：

```sql
detected_title = ?,
title = CASE WHEN title_is_manual = 1 THEN title ELSE ? END,
detected_version = ?,
version = CASE WHEN version_is_manual = 1 THEN version ELSE ? END
```

参数中的检测标题和版本必须分别复用同一次解析结果。

- [x] **步骤 6：写并实现确认移动保护测试**

创建已手动编辑标题和版本的游戏，改变目录位置并走现有确认移动流程；断言游戏 ID、标题、版本、两个手动标记都保留，新 `relative_dir` 生效，`detected_title` 和 `detected_version` 来自新目录名。

确认移动 SQL 不更新 `title`、`version`、`title_is_manual`、`version_is_manual`。

- [x] **步骤 7：运行任务 4 门禁**

```powershell
.venv\python.exe -m pytest tests/integration/scanning/test_scan_service.py -q
.venv\python.exe -m ruff check src/gamesave_scout/scanning src/gamesave_scout/library/title_parser.py tests/integration/scanning/test_scan_service.py
.venv\python.exe -m mypy src
git diff --check
```

- [x] **步骤 8：提交扫描协调改动**

```powershell
git add src/gamesave_scout/scanning/service.py src/gamesave_scout/scanning/reconcile.py tests/integration/scanning/test_scan_service.py
git commit -m "feat: 扫描时分离并保护游戏版本号"
```

---

### 任务 5：游戏库前端编辑、展示与检索

**文件：**

- 修改：`frontend/src/api/contracts.ts`
- 修改：`frontend/src/api/mockBridge.ts`
- 修改：`frontend/src/features/library/GameSettingsPanel.vue`
- 修改：`frontend/src/features/library/GameCard.vue`
- 修改：`frontend/src/features/library/GameDetailDrawer.vue`
- 修改：`frontend/src/features/library/libraryFilters.ts`
- 修改：`frontend/src/features/library/library.css`
- 修改：`frontend/tests/GameSettingsPanel.spec.ts`
- 修改：`frontend/tests/GameGrid.spec.ts`
- 修改：`frontend/tests/libraryFilters.spec.ts`

**接口：**

- `Game.version: string | null`
- `GameSaveScoutBridge.set_game_metadata(input: { gameId: string; title: string; version: string | null }): Promise<ApiResult<Game>>`
- 设置面板一次提交两个输入框。

- [x] **步骤 1：扩展类型夹具并写设置面板失败测试**

在契约和 mock 游戏中加入 `version`；把 mock bootstrap 的当前 `schemaVersion` 改为 2。测试设置面板：

```ts
expect(bridge.set_game_metadata).toHaveBeenCalledWith({
  gameId: game.id,
  title: '自定义标题',
  version: 'Ver 2.0',
})
```

另测空版本发送 `null`；桥接失败时两个输入框仍保留用户刚输入的值并显示错误。

- [x] **步骤 2：运行设置面板测试并确认类型或断言失败**

```powershell
.venv\npm.cmd --prefix frontend run test:unit -- --run frontend/tests/GameSettingsPanel.spec.ts
```

- [x] **步骤 3：实现标题与版本号双输入原子保存**

`GameSettingsPanel.vue` 增加 `version = ref("")`，监听当前游戏时同步 `game.version ?? ""`。输入控件使用：

```html
<input data-test="game-title-input" v-model="title" />
<input data-test="game-version-input" v-model="version" />
<button data-test="save-game-metadata" @click="saveMetadata">保存</button>
```

保存时调用一次 `set_game_metadata`；前端只做首尾清理，不自行运行自动拆分器。

- [x] **步骤 4：写卡片和详情展示失败测试**

- 有版本号时，游戏卡片标题下方显示独立 `.game-version`。
- 详情抽屉标题旁显示 `.detail-version-badge`。
- 版本为 `null` 时两个版本元素都不存在，标题布局不留下空占位。
- 长标题和长版本号通过换行或省略保持卡片和抽屉不横向溢出。

- [x] **步骤 5：实现独立展示样式**

游戏卡片不得把标题和版本拼成一个可搜索标题节点；详情使用标题容器和版本徽标：

```html
<div class="detail-title-row">
  <h2>{{ game.title }}</h2>
  <span v-if="game.version" class="detail-version-badge">{{ game.version }}</span>
</div>
```

在 `library.css` 中为 `.game-version`、`.detail-title-row`、`.detail-version-badge` 设置可换行、`min-width: 0` 和窄窗口规则。

- [x] **步骤 6：写并实现版本号检索测试**

`frontend/tests/libraryFilters.spec.ts` 增加：只输入 `1.0.8` 可匹配标题为 `AoiChan`、版本为 `v1.0.8` 的游戏；大小写不敏感；无版本号不报错。

```ts
const matchesQuery =
  game.title.toLocaleLowerCase().includes(query) ||
  (game.version ?? '').toLocaleLowerCase().includes(query)
```

保留现有路径、状态或其他筛选语义，不改变结果排序。

- [x] **步骤 7：运行任务 5 门禁**

```powershell
.venv\npm.cmd --prefix frontend run test:unit -- --run frontend/tests/GameSettingsPanel.spec.ts frontend/tests/GameGrid.spec.ts frontend/tests/libraryFilters.spec.ts
.venv\npm.cmd --prefix frontend run type-check
git diff --check
```

- [x] **步骤 8：提交游戏库前端**

```powershell
git add frontend/src/api/contracts.ts frontend/src/api/mockBridge.ts frontend/src/features/library/GameSettingsPanel.vue frontend/src/features/library/GameCard.vue frontend/src/features/library/GameDetailDrawer.vue frontend/src/features/library/libraryFilters.ts frontend/src/features/library/library.css frontend/tests/GameSettingsPanel.spec.ts frontend/tests/GameGrid.spec.ts frontend/tests/libraryFilters.spec.ts
git commit -m "feat: 在游戏库分离显示标题与版本号"
```

---

### 任务 6：批量封面队列展示与 VNDB 纯标题查询

**文件：**

- 修改：`src/gamesave_scout/covers/candidates.py`
- 修改：`src/gamesave_scout/covers/wizard_service.py`
- 修改：`src/gamesave_scout/bridge/api.py`
- 修改：`tests/integration/covers/test_cover_wizard_service.py`
- 修改：`tests/unit/bridge/test_cover_wizard_api.py`
- 修改：`frontend/src/api/contracts.ts`
- 修改：`frontend/src/features/covers/CoverWizardQueue.vue`
- 修改：`frontend/src/features/covers/CoverWizardWorkspace.vue`
- 修改：`frontend/src/features/covers/CoverCandidateToolbar.vue`
- 修改：`frontend/tests/CoverWizardQueue.spec.ts`
- 修改：`frontend/tests/CoverWizardWorkspace.spec.ts`
- 修改：`frontend/tests/coverWizardStore.spec.ts`
- 修改：`frontend/tests/App.spec.ts`

**接口与隐私边界：**

- `CoverWizardQueueItem.version: str | None`，前端对应 `string | null`。
- VNDB 的查询参数始终是 `game.title`。
- 本地队列、当前游戏标题和任务进度可以显示 `title + version`，但拼接值不得传给 VNDB 客户端。

- [x] **步骤 1：写 VNDB 查询参数失败测试**

让集成测试的假 VNDB 客户端记录每次 `search(title=...)`；把测试游戏保存为标题 `Alice`、版本 `v1.0.8`，然后分别执行当前游戏和全部游戏搜索：

```python
assert fake_vndb.queries == ["Alice"]
assert snapshot.queue[0].title == "Alice"
assert snapshot.queue[0].version == "v1.0.8"
```

批量搜索有多个版本化游戏时逐项断言所有查询都不含对应版本号。

- [x] **步骤 2：运行封面向导测试并确认队列缺少版本字段**

```powershell
.venv\python.exe -m pytest tests/integration/covers/test_cover_wizard_service.py tests/unit/bridge/test_cover_wizard_api.py -q
```

- [x] **步骤 3：扩展后端队列模型和 DTO**

在 `CoverWizardQueueItem` 的必填字段之后、带默认值状态字段之前加入：

```python
version: str | None = None
```

若数据类字段顺序要求默认字段位于末尾，则放在 `initial_has_cover` 之后并保持后续字段均有默认值。`CoverWizardService.start()` 从 `Game.version` 填充；`_cover_wizard_snapshot_dto()` 输出 `version`。

- [x] **步骤 4：把查询值和本地显示值明确分开**

保留生产查询：

```python
candidates = self._vndb.search(game.title, limit, session_root, game.id, context)
```

增加只供本地消息使用的辅助函数：

```python
def _game_display_name(game: Game) -> str:
    if game.version:
        return f"{game.title} {game.version}"
    return game.title
```

进度、完成和单游戏错误文字使用 `_game_display_name(game)`；不得把该函数返回值作为 `search()` 的第一个参数。

- [x] **步骤 5：写前端队列和工作区展示失败测试**

- 队列常规卡片和窄窗口选择器都把版本号作为独立次要文字。
- 当前游戏标题旁显示版本徽标。
- 版本为 `null` 时不渲染空标签。
- 工具栏隐私文字为“只发送游戏标题，不发送版本号、安装路径或本地文件”。

同时给 `App.spec.ts`、`coverWizardStore.spec.ts` 等直接构造的 `CoverWizardQueueItem` 夹具补 `version: null`，保持 TypeScript 严格检查通过。

- [x] **步骤 6：实现前端展示和隐私说明**

`CoverWizardQueue.vue` 与 `CoverWizardWorkspace.vue` 不修改队列项目的 `title`；使用单独节点显示 `item.version`。复用任务 5 的版本徽标视觉语义，避免重新把两个字段拼入一个可编辑值。

- [x] **步骤 7：运行任务 6 门禁**

```powershell
.venv\python.exe -m pytest tests/integration/covers/test_cover_wizard_service.py tests/unit/bridge/test_cover_wizard_api.py -q
.venv\python.exe -m ruff check src/gamesave_scout/covers src/gamesave_scout/bridge/api.py tests/integration/covers/test_cover_wizard_service.py tests/unit/bridge/test_cover_wizard_api.py
.venv\python.exe -m mypy src
.venv\npm.cmd --prefix frontend run test:unit -- --run frontend/tests/CoverWizardQueue.spec.ts frontend/tests/CoverWizardWorkspace.spec.ts frontend/tests/coverWizardStore.spec.ts frontend/tests/App.spec.ts
.venv\npm.cmd --prefix frontend run type-check
git diff --check
```

- [x] **步骤 8：提交批量封面和 VNDB 改动**

```powershell
git add src/gamesave_scout/covers/candidates.py src/gamesave_scout/covers/wizard_service.py src/gamesave_scout/bridge/api.py tests/integration/covers/test_cover_wizard_service.py tests/unit/bridge/test_cover_wizard_api.py frontend/src/api/contracts.ts frontend/src/features/covers/CoverWizardQueue.vue frontend/src/features/covers/CoverWizardWorkspace.vue frontend/src/features/covers/CoverCandidateToolbar.vue frontend/tests/CoverWizardQueue.spec.ts frontend/tests/CoverWizardWorkspace.spec.ts frontend/tests/coverWizardStore.spec.ts frontend/tests/App.spec.ts
git commit -m "feat: VNDB 仅按游戏标题检索封面"
```

---

### 任务 7：固定文档、生成资源和完整质量门禁

**文件：**

- 修改：`docs/superpowers/plans/2026-08-12-GameSave-Scout-01-基础架构.md`
- 修改：`docs/superpowers/plans/2026-08-12-GameSave-Scout-02-游戏库扫描与启动.md`
- 修改：`docs/superpowers/plans/2026-08-12-GameSave-Scout-03-封面与游戏库界面.md`
- 修改：`docs/superpowers/plans/2026-08-12-GameSave-Scout-07-便携版打包与发布.md`
- 修改：`docs/superpowers/plans/2026-08-12-GameSave-Scout-开发路线图.md`
- 修改：`resources/ui/index.html`
- 修改：`resources/ui/assets/*`
- 修改：本实施计划的状态和复选框。

**产出：**

- 固定文档从“已设计”更新为经过测试的实际实现事实。
- 生产前端重新构建到 `resources/ui`。
- 完整 Python、前端、源码冒烟和文本质量门禁证据。

- [x] **步骤 1：先运行受影响区域回归**

```powershell
.venv\python.exe -m pytest tests/unit/db tests/unit/library tests/integration/scanning tests/integration/covers tests/unit/bridge/test_library_api.py tests/unit/bridge/test_cover_wizard_api.py -q
.venv\npm.cmd --prefix frontend run test:unit -- --run frontend/tests/GameSettingsPanel.spec.ts frontend/tests/GameGrid.spec.ts frontend/tests/libraryFilters.spec.ts frontend/tests/CoverWizardQueue.spec.ts frontend/tests/CoverWizardWorkspace.spec.ts
```

预期：全部通过后才进入生产构建。

- [x] **步骤 2：构建生产前端资源**

```powershell
.venv\npm.cmd --prefix frontend run build
```

确认 `resources/ui/index.html` 引用本次生成的新哈希资源；清单中没有已被新构建淘汰的旧哈希文件。

- [x] **步骤 3：运行 Python 全量门禁**

```powershell
.venv\python.exe -m pytest
.venv\python.exe -m ruff check src tests scripts
.venv\python.exe -m mypy src scripts
```

预期：零失败、零 Ruff 诊断、零 mypy 错误；记录实际 pytest 项数和耗时。

- [x] **步骤 4：运行前端全量门禁**

```powershell
.venv\npm.cmd --prefix frontend run test:unit -- --run
.venv\npm.cmd --prefix frontend run type-check
.venv\npm.cmd --prefix frontend run build
```

预期：零失败且生产构建成功；记录实际测试文件数、测试数和耗时。

- [x] **步骤 5：运行 schema 2 源码冒烟和差异检查**

```powershell
.venv\python.exe -m gamesave_scout --smoke-test
git diff --check
git status --short
git diff --stat
```

预期：源码冒烟输出成功信息且数据库版本为 2；工作区只包含本功能、生成前端和对应中文固定文档。

- [x] **步骤 6：把实现事实写回固定文档**

- 模块 01：数据库版本改为 2，列出三个新字段、schema 1 不回填旧标题的升级语义和实际门禁。
- 模块 02：把标题/版本号分离从待实现改为已实现，记录保守拆分、独立手动保护和重扫行为。
- 模块 03：记录游戏卡片、详情、批量封面队列的独立版本展示，以及 VNDB 只发送标题。
- 模块 07：冒烟示例的 `schemaVersion` 改为 2，关键迁移载荷同时列出 `0001_initial.sql` 和 `0002_initial.sql`。
- 开发路线图：更新模块 01、02、03 状态、完成记录和实际测试数量，不把未执行的人工检查写成已通过。

- [x] **步骤 7：更新本实施计划完成状态**

把已实际完成的复选框从 `[ ]` 改为 `[x]`，在标题下增加一行实施状态，写入真实的 Python 与前端测试数量、静态检查、构建和冒烟结果；任何尚未人工执行的项目必须明确标为待验收。

- [x] **步骤 8：提交文档和生成资源**

```powershell
git add docs/superpowers/plans docs/历史归档/2026-08/2026-08-17-标题与版本号分离实施计划.md frontend/tests/bridge.spec.ts resources/ui
git commit -m "docs: 完成标题与版本号分离"
```

- [x] **步骤 9：核对最终历史和工作区**

```powershell
git status --short
git log -7 --oneline
```

预期：工作区干净，最近提交依次覆盖解析器、schema、元数据、扫描、游戏库前端、VNDB 队列和最终文档；未经用户明确要求不执行 `git push`。

---

## 实现后人工验收清单

下列项目不由实现过程自动删除或修改用户数据，完成自动门禁后交给用户验收：

- [x] 关闭 GameSave Scout 后，由用户自行删除可舍弃的 `data/library.db` 并重新启动。
- [x] 扫描 `AoiChan.v1.0.8`、`Game Name - Ver 1.42`，确认标题和版本号自动分离。
- [x] 扫描 `Need for Speed 21 Heat`、`Resident Evil 4`、`Game 1.05`，确认没有误拆。
- [x] 在详情设置中分别编辑标题和版本、手动清空版本，重扫后确认手动结果保留。
- [x] 用版本化目录名执行 VNDB 当前游戏搜索和全部游戏搜索，确认能按纯标题得到候选。
- [x] 在 80%、100%、120% 缩放和窄窗口下检查卡片、详情抽屉、批量封面队列无重叠和横向溢出。
