# GameShelf 固定文档与 README 状态同步设计

> 文档性质：一次性文档整理设计；日期：2026-08-17；状态：已获用户确认，等待实施。

## 1. 目标

清理三个固定模块文档中已经被后续实现或路线图结论覆盖的旧状态，并把根目录 README 更新为兼顾普通用户与开发者的当前项目入口。此次工作只修改 Markdown，不改变代码、配置、数据库、生产 UI 或本地便携包。

## 2. 固定文档调整

### 2.1 模块 01

从“后续工作”删除正式 PyInstaller onedir 和干净环境验证待办，因为模块 07 已完成当前 V0.1 发布流水线、本机复核和 Windows Sandbox 主闭环。继续保留数据库结构变化必须新增迁移文件的长期约束，并在变更记录中说明本次状态同步。

### 2.2 模块 05

把引导式寻找存档及人工审核移动到 V0.1 已实现范围，删除“V0.1 待实现范围”小节。V0.2 孤立存档和 V0.3 备份、恢复、同步仍保持后续版本定位；模块 06 继续作为引导式检测的详细事实来源。

### 2.3 模块 06

把顶部状态改为 V0.1 当前范围完成。完整目标 Windows 10/11 设备矩阵没有执行，不记为已通过，但按当前路线图属于后续可选兼容性复核，不再阻塞 V0.1；V0.2 孤立存档仍需在实施前单独冻结设计。

## 3. README 调整

README 保持当前“产品介绍—主要功能—界面预览—便携使用—开发构建—文档许可”结构，不进行整篇重写：

- 在主要功能中补充主页批量管理、批量封面四类候选来源、固定控制区、内部独立滚动和深色滚动条。
- 明确 VNDB 默认关闭，只发送游戏标题，所有候选都要由用户人工采用。
- 增加简短的当前状态说明：V0.1 源码范围完成，额外目标设备矩阵为可选复核，V0.2/V0.3 功能尚未实现。
- 保留现有双版本便携包使用方式、Conda 开发环境、完整门禁、发布构建和调试命令。
- 在文档入口补充模块 03、05、06 链接；不加入容易随提交变化的测试数量。
- 不更新现有截图，也不重建或提交 `dist` 便携包。

## 4. 验收与提交

实施后验证：

1. 三份固定模块文档不再包含上述过时状态，且与开发路线图一致。
2. README 对联网、隐私、V0.1 当前范围和后续版本的描述不矛盾。
3. 遍历 README 与 `docs/**/*.md`，本地 Markdown 链接失效数为 0。
4. `git diff --check` 通过，差异只包含批准的 Markdown 文件。
5. 文档修改提交到当前 `main`，随后推送 `origin/main`；不运行便携包重建。
