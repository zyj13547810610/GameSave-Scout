# GameShelf 固定文档与 README 状态同步实施计划

> 实施状态：2026-08-17 已完成。已检查 README 与 `docs/**/*.md` 共 34 份 Markdown，本地链接失效数为 0；归档前复核 Python 623 项、前端 32 个测试文件共 114 项全部通过。主体文档提交 `60440e8` 已推送到 `origin/main`，本完成状态随最终归档提交同步。未修改或重建便携包。

> **供智能代理执行：** 必须使用 `superpowers:executing-plans` 按任务顺序实施；项目明确不使用子代理。步骤使用复选框跟踪。

**目标：** 清理模块 01、05、06 的过时状态，并把 README 更新为兼顾用户和开发者的当前项目入口，完成后提交并推送 `main`。

**架构：** 本次只同步 Markdown 事实，不修改代码、配置、数据库、生产 UI 或本地发布产物。固定模块文档记录当前事实，README 提供稳定概览和操作入口，一次性设计与实施记录保留在 `docs/历史归档/2026-08`。

**技术栈：** Markdown、PowerShell、Git。

## 全局约束

- 直接在当前本地 `main` 工作，不创建分支或 worktree，不使用子代理。
- 不修改或提交 `build/`、`dist/`、`data/`、`webview安装包/` 和用户文件。
- 不重建便携包，不写入容易随提交变化的测试数量。
- 固定文档与 README 使用中文；历史变更记录可以保留当时事实，但当前状态和后续工作不得互相矛盾。
- 完成后提交全部批准的 Markdown 修改并推送 `origin/main`。

---

### 任务 1：同步模块 01、05、06 当前状态

**文件：**

- 修改：`docs/superpowers/plans/2026-08-12-GameShelf-01-基础架构.md`
- 修改：`docs/superpowers/plans/2026-08-12-GameShelf-05-静态存档位置.md`
- 修改：`docs/superpowers/plans/2026-08-12-GameShelf-06-动态与孤立存档发现.md`

**接口：**

- 消费：开发路线图中“V0.1 当前范围完成、额外目标系统矩阵为后续可选复核”的当前结论。
- 产出：三份固定模块文档与路线图一致的当前状态和后续工作。

- [x] **步骤 1：修正模块 01 后续工作**

删除“为便携发布加入正式 PyInstaller onedir 配置和干净虚拟机验证”这一已完成待办，保留数据库迁移长期约束，并在 2026-08-17 变更记录说明发布职责已由模块 07 落地。

- [x] **步骤 2：修正模块 05 V0.1 范围**

把以下两项写入“V0.1 已实现范围”：

- 用户主动开始引导式检测，结合一次实际保存查找未知存档。
- 候选必须经过用户审核后才能成为正式存档位置。

删除“V0.1 待实现范围”小节，把顶部最后更新改为 2026-08-17，并保留 V0.2/V0.3 后续版本内容。

- [x] **步骤 3：修正模块 06 顶部状态**

顶部状态明确写为：V0.1 当前范围完成；完整目标 Windows 10/11 设备矩阵未执行且不记为通过，但已经转为后续可选兼容性复核，不阻塞 V0.1；V0.2 孤立存档的最终设计仍需实施前单独确认。

- [x] **步骤 4：检查三份固定文档的当前状态关键词**

运行：

```powershell
rg -n "待实现|首发待办|为便携发布加入正式 PyInstaller" docs/superpowers/plans/2026-08-12-GameShelf-01-基础架构.md docs/superpowers/plans/2026-08-12-GameShelf-05-静态存档位置.md docs/superpowers/plans/2026-08-12-GameShelf-06-动态与孤立存档发现.md
```

预期：当前状态和后续工作中不再出现三处过时表述；历史变更记录中的历史事实允许保留。

---

### 任务 2：平衡更新 README

**文件：**

- 修改：`README.md`

**接口：**

- 消费：当前开发路线图、模块 03 批量封面与视口壳层、模块 05/06 存档功能、模块 07 双版本构建方式。
- 产出：兼顾普通用户和开发者、且不依赖瞬时测试数字的仓库入口文档。

- [x] **步骤 1：补充主要功能**

在主要功能列表中写明：

- 主页批量管理与事务式处理。
- 批量封面支持 VNDB、拖放/粘贴、浅层扫描和自定义非递归目录，全部候选人工采用。
- VNDB 默认关闭，只发送游戏标题。
- 主页与批量封面使用固定控制区、内部独立滚动和深色滚动条。

- [x] **步骤 2：增加当前项目状态**

在主要功能之后增加“当前状态”小节：V0.1 源码范围完成；额外目标 Windows 10/11 与特殊故障矩阵为可选复核；V0.2 孤立存档和 V0.3 备份/恢复/同步尚未实现。不得写入当前测试数量或声称未执行矩阵已经通过。

- [x] **步骤 3：补全长期文档入口**

在“项目文档与许可证”中增加模块 03、模块 05、模块 06 的链接，保留总体设计、路线图、模块 07、许可证和第三方声明。

- [x] **步骤 4：检查 README 命令和范围描述**

确认现有 Conda、前端构建、源码启动、双版本发布命令未被改坏；README 不声称当前本地 `dist` 已包含最新提交，也不要求本次重建便携包。

---

### 任务 3：验证、归档、提交并推送

**文件：**

- 修改：`docs/历史归档/2026-08/2026-08-17-固定文档与README状态同步实施计划.md`

**接口：**

- 消费：任务 1、2 的最终 Markdown。
- 产出：失效链接为 0、干净本地工作区和同步后的 `origin/main`。

- [x] **步骤 1：检查全部本地 Markdown 链接**

使用 PowerShell 遍历 `README.md` 与 `docs/**/*.md`，解析非 HTTP、非邮件、非锚点的 `.md` 链接，基于来源文件目录检查目标是否存在。预期失效链接为 0。

- [x] **步骤 2：检查差异范围和格式**

运行：

```powershell
git diff --check
git status --short
git diff -- README.md docs/superpowers/plans/2026-08-12-GameShelf-01-基础架构.md docs/superpowers/plans/2026-08-12-GameShelf-05-静态存档位置.md docs/superpowers/plans/2026-08-12-GameShelf-06-动态与孤立存档发现.md docs/历史归档/2026-08/2026-08-17-固定文档与README状态同步实施计划.md
```

预期：除已经提交的设计记录外，只包含上述五份批准的 Markdown 修改，不包含代码、资源、数据或发布产物。

- [x] **步骤 3：更新本计划完成状态**

把顶部状态补充为 2026-08-17 已完成，勾选所有步骤，并记录 Markdown 文件检查数量、失效链接 0、提交与推送结果。

- [x] **步骤 4：提交文档修改**

```powershell
git add -- README.md docs/superpowers/plans/2026-08-12-GameShelf-01-基础架构.md docs/superpowers/plans/2026-08-12-GameShelf-05-静态存档位置.md docs/superpowers/plans/2026-08-12-GameShelf-06-动态与孤立存档发现.md docs/历史归档/2026-08/2026-08-17-固定文档与README状态同步实施计划.md
git commit -m "docs: 同步项目状态与README"
```

- [x] **步骤 5：推送并确认远端同步**

```powershell
git push origin main
git status -sb
```

预期：推送成功，`main` 与 `origin/main` 同步且工作区干净；不重建便携包。
