# GameShelf 引导式与孤立存档发现

> 文档性质：当前与后续版本模块设计；适用版本：V0.1～V0.2；最后更新：2026-08-17；状态：V0.1 引导式寻找存档当前范围已完成，并通过自动验证及普通保存、纯自动保存、取消、关闭处理、多缩放、窄窗口和启动器脱离降级的本机人工验收；完整目标 Windows 10/11 设备矩阵未执行且不记为通过，但已转为后续可选兼容性复核，不再阻塞 V0.1；V0.2 孤立存档发现的最终设计仍需在实施前单独确认。

## 1. 版本范围

当前版本划分：

- V0.1 包含手动存档位置、按需静态建议和用户主动启动的“引导式寻找存档”。
- V0.2 实现已删除游戏留下的“孤立存档发现与反推”。
- V0.3 或更晚再考虑存档备份、恢复、同步和版本管理。

引导式寻找存档是 V0.1 发布门槛，但它不是常驻后台监控器：GameShelf 启动时不自动监控，用户日常启动游戏时也不默认采集文件变化。只有用户在单个游戏详情中明确确认开始，才创建一次受限会话。

V0.1 和 V0.2 是两个独立子项目。本轮只实施 V0.1，不提前加入孤立存档扫描、仅存档卡片审核或全盘反推能力。

## 2. V0.1 用户操作流程

现有“查找存档”和新增“引导式寻找”是两个独立入口：

- “查找存档”只在用户点击后查询自定义清单、Ludusavi 索引和引擎提示。
- “引导式寻找”创建文件变化检测会话，并由向导启动游戏。

标准操作流程：

1. 用户在游戏详情的“存档位置”区域点击“引导式寻找”。
2. GameShelf 展示启动程序、默认监控范围、不可用范围、定向注册表键和隐私说明。
3. 用户调整勾选范围并点击“确认开始”。
4. GameShelf 建立观察器；所有观察器就绪后，使用现有安全启动配置启动游戏。
5. 用户进入游戏，创建或覆盖一次存档。
6. 用户回到 GameShelf，点击“我刚刚保存了”。
7. GameShelf 记录保存时间，再等待约 3 秒接收延迟写盘，然后自动停止监控并分析变化。
8. 用户审核候选位置；只有明确接受后才写入 `save_locations`，来源为 `dynamic`。

普通流程不要求用户再点击一次“停止并分析”。对于只有自动存档、无法准确标记保存时间的游戏，用户可以直接点击“停止并分析”；此时仍分析整个会话，但结果整体更保守且不默认勾选。

关闭游戏详情抽屉不会结束会话。主页显示当前游戏、阶段和持续时间，用户可以返回向导。全程序同时最多只有一个活动引导会话。

## 3. V0.1 后台执行流程

后台按以下顺序执行：

1. **范围预览**：验证游戏状态、主程序、当前活动会话、默认目录可访问性和定向注册表规则；此时不启动监控。
2. **会话准备**：创建 `preparing` 会话，规范化并去重用户批准的根目录，建立轻量元数据和定向注册表基线，启动全部文件观察器。
3. **启动游戏**：观察器全部就绪后调用现有 `GameLauncher`，记录根 PID，状态进入 `monitoring`。
4. **事件聚合**：将创建、修改、移动和删除事件按 Windows 路径合并，只保留路径、事件类型、接收时间和受限文件元数据。
5. **保存标记**：记录“我刚刚保存了”的时间，继续观察约 3 秒后进入 `settling`。
6. **收尾补偿**：停止观察器，获取定向注册表结束快照；只有溢出或观察器失败的根目录才执行受限元数据补扫。
7. **过滤评分**：过滤噪声、合并目录候选、计算可信度并生成人能理解的证据。
8. **持久化审核项**：以事务写入最终候选和会话摘要，释放内存原始事件，会话进入 `completed`。
9. **接受候选**：用户明确接受后，复用存档位置服务折叠便携模板并写入正式位置。

正常监控不会递归建立完整 AppData 快照，也不会周期性遍历 AppData。文件变化由 Windows 事件接口被动报告。

## 4. 默认范围与用户批准

启动前默认勾选：

- 当前游戏安装目录。
- Documents。
- Saved Games。
- AppData Roaming、Local 和 LocalLow。
- 已确认存档位置的父目录。

ProgramData 默认不勾选，只作为可选范围展示。用户可以在确认开始前添加其他本机目录。额外目录必须位于 `<game>`、`<home>` 或现有 Windows 已知文件夹令牌能够表示的范围内；无法折叠为便携路径模板的目录在 V0.1 中明确拒绝，避免检测完成后产生无法保存或无法迁移的候选。

路径规范化后去重，嵌套范围只建立一个观察器。不可访问目录明确展示并跳过，不请求管理员权限。用户明确选择的目录如果在确认后失效，则本次启动失败，不静默缩小用户批准的范围。

V0.1 只支持本机目录。UNC 和网络共享显示为不支持；用户仍可通过手动添加维护网络存档位置。

符号链接、目录联接和其他 Windows 重解析点不能让事件或补扫越过用户批准的范围。观察器、聚合器和补扫器都必须执行边界验证。

注册表只比较以下特定键：

- 当前游戏的 Ludusavi、自定义清单等静态规则明确给出的键。
- 用户已经确认的注册表存档位置。

不得遍历整个注册表。注册表快照只保存键和值的名称、类型、长度和摘要，不持久化原始值。

## 5. 组件边界

| 组件 | 职责 |
| --- | --- |
| `GuidedSaveSessionService` | 编排单活动会话、状态转换、启动游戏、保存标记、停止、取消和关闭清理 |
| `WindowsDirectoryWatcher` | 在批准根目录中使用 Win32 事件接口采集变化并明确报告溢出 |
| `BoundedMetadataScanner` | 只对溢出或失败范围执行有深度、条目数和时间限制的补扫 |
| `WindowsProcessTreeTracker` | 判断向导启动的进程树是否仍存在，不进行文件写入归因 |
| `RegistryMetadataReader` | 比较批准注册表键的元数据摘要 |
| `GuidedChangeAggregator` | 规范化路径、合并重复事件和记录时间证据 |
| `score_guided_changes_with_summary` | 过滤噪声、合并目录、评分并生成解释性证据 |
| 会话与候选仓库 | 读写 `save_detection_sessions` 和 `save_discoveries` |
| 桥接接口 | 校验预览、开始、标记、停止、取消、审核、接受和活动会话关闭选择请求 |
| Vue 向导与状态栏 | 范围确认、会话进度、保存标记、警告和候选审核 |

引导会话不占用现有 `TaskRegistry` 的通用工作线程。监控可能持续数十分钟，若占用当前两个通用工作线程之一，会影响游戏扫描和 Ludusavi 更新。引导服务使用隔离的观察线程和受控生命周期。

## 6. Windows 监控与进程跟踪

V0.1 使用项目内自建的窄范围 Win32 适配器：

- 通过 `ctypes` 调用 `ReadDirectoryChangesW`，不新增 Watchdog、watchfiles 或 pywin32 运行依赖。
- 每个去重后的批准根目录拥有独立句柄和固定容量事件缓冲。
- Windows 返回零字节或适配器检测到队列容量用尽时，将对应根目录标记为溢出。
- 停止和取消使用可中断 I/O；若取消请求恰逢下一次读取尚未挂起，停止流程在原有总超时内重试取消，直到关闭句柄并确认观察线程退出。
- 进程树通过 Windows Tool Help 进程快照跟踪根 PID 及其子进程。

文件系统事件通常不能可靠证明由哪个 PID 写入，因此进程树只控制会话生命周期，不参与文件归因或候选加分。

进程树可靠退出时，立即停止观察并进入自动分析；此时整个已观察进程树均已退出，不再额外等待 3 秒。只有用户点击“我刚刚保存了”后才保留约 3 秒延迟写盘等待。如果启动器很快退出且无法继续跟踪实际游戏，显示“无法可靠判断游戏是否仍在运行”，保持监控并等待用户点击“我刚刚保存了”或“停止并分析”。

## 7. 会话状态与交互

状态定义：

- `preparing`：验证范围、建立轻量基线和启动观察器；游戏尚未启动。
- `monitoring`：观察器就绪且游戏已启动，等待保存标记或手动停止。
- `settling`：保存标记后等待约 3 秒，或在手动停止、可靠进程退出后立即执行补扫和评分。
- `completed`：候选已持久化，可以立即或稍后审核。
- `cancelled`：用户取消，未生成候选。
- `failed`：准备、启动、监控或分析失败。
- `interrupted`：GameShelf 异常退出后，下次启动识别到的残留活动会话。

数据库约束和服务锁共同保证全程序最多一个 `preparing`、`monitoring` 或 `settling` 会话。

“停止并分析”保留已采集变化并生成候选；“取消会话”清理观察器且不生成候选。点击“我刚刚保存了”后重复提交相同请求是幂等的，不会创建多个收尾操作。

已完成但尚未审核的候选保留在数据库中。再次进入该游戏详情时可以继续审核，或明确丢弃旧结果后开始新会话。

正常退出 GameShelf 时如有活动会话，提示“停止并分析”“取消会话”或“返回程序”。异常退出、断电或强制终止不尝试跨重启恢复观察器；下次启动将残留状态改为 `interrupted`。

## 8. 资源上限

V0.1 固定以下上限，不提供高级配置界面：

| 项目 | 上限 |
| --- | --- |
| 活动会话时长 | 30 分钟 |
| 保存标记后的等待 | 约 3 秒 |
| 单会话不同变化路径 | 20,000 |
| 内存事件队列 | 50,000 条 |
| 过滤前候选 | 500 项 |
| 最终展示候选 | 200 项 |
| 单个补扫根目录深度 | 8 层 |
| 单个补扫根目录条目 | 100,000 项 |
| 单个补扫根目录时间 | 5 秒 |

重复事件直接聚合，不随事件次数线性增长。队列或路径上限触发后标记对应根目录溢出，不继续无限增长。

30 分钟只是用户忘记返回时的防遗忘上限。到期后自动进入 `settling` 并分析已有变化。正常流程在点击“我刚刚保存了”后的约 3 秒等待结束后立即分析。

补扫只检查路径、创建时间、修改时间和大小，只保留会话时间窗口内的变化。深度、条目或时间上限触发后显示“结果可能不完整”。

## 9. 聚合、过滤与评分

候选使用可解释的确定性规则评分，不使用不透明模型。

主要加分依据：

| 证据 | 影响 |
| --- | --- |
| 在保存标记前后 5 秒内变化 | 强加分 |
| 距离保存标记超过 5 秒但不超过 15 秒 | 中等加分 |
| 距离保存标记超过 15 秒 | 保留候选，不予时间加分 |
| 同一目录内多个文件协调变化 | 加分 |
| 与当前游戏静态规则一致 | 加分 |
| 位于游戏目录、确认位置父目录或可信引擎结构 | 加分 |
| 文件名或文件组合具有存档特征 | 小幅加分 |

点击“我刚刚保存了”后仍固定等待约 3 秒以接收延迟写盘；这段时间内的变化自然落入前后 5 秒的强加分窗口。评分仍分析整个监控会话，不会删除超过 15 秒的变化，只是不为其提供时间加分。单靠扩展名不能产生高可信建议。无保存标记时可以分析整个会话，但候选整体降权且不默认勾选。

过滤与降权规则：

- 明确的日志、缓存、临时目录、着色器、遥测和崩溃转储直接过滤或强降权。
- 浏览器、聊天软件等其他程序产生的高频变化降权。
- 只修改访问时间、最终文件不存在或只出现删除事件的路径不单独成为候选。
- “临时文件创建后重命名覆盖”的原子保存合并到最终文件。
- 已经存在于当前游戏正式存档位置中的路径不再作为新候选。

同一目录中多个相关文件优先合并为目录候选。只有单个文件具有明显独立存档特征时才生成文件候选。候选按 Windows 路径去重。

结果分组：

- 高可信位置：`confidence >= 0.85`。
- 可能的位置：`0.60 <= confidence < 0.85`。
- 低可信变化：`confidence < 0.60`，默认折叠。
- 注册表候选：独立分组，永不默认勾选。

高可信文件系统候选可以默认勾选。没有保存标记、发生溢出或补扫截断时，任何候选都不默认勾选。

每项候选至少展示路径、可信度、与保存标记的时间关系、代表性变化文件、静态或引擎证据，以及溢出、补扫和截断状态。代表性文件只展示有限数量。

## 10. 数据结构与开发期建库策略

当前仍处于开发阶段，不为现有临时数据库增加迁移兼容代码。实现时直接修改 `src/gameshelf/db/migrations/0001_initial.sql` 中的最终表结构，schema 版本暂时保持 1。

实施前确认 GameShelf 进程已经关闭，将现有 `data/library.db`、`data/library.db-wal` 和 `data/library.db-shm` 移动到 `data/db_backups` 中一个独立的时间戳目录，然后让 GameShelf 创建新数据库。测试数据库继续从修改后的初始脚本全新创建。

`save_detection_sessions` 保存：

- 会话和游戏 ID。
- 状态及开始监控、保存标记、结束时间。
- 向导启动的根 PID。
- 用户批准的范围。
- 不可访问、溢出和截断范围摘要。
- 进程跟踪是否降级。
- 候选、过滤等结果计数。
- 稳定错误代码和可读错误摘要。

部分唯一索引保证全库最多一个活动会话。应用启动时把残留活动状态改为 `interrupted`。

`save_discoveries` 保存：

- 所属检测会话。
- 路径模板、显示路径和 Windows 去重键。
- 目录、文件或注册表类型。
- 可信度、结构化证据和代表性变化文件。
- 首次、最后变化时间及与保存标记的时间差。
- 是否受到溢出或截断影响。
- 是否满足安全默认勾选条件。
- `unreviewed`、`accepted` 或 `ignored` 审核状态。
- 接受后创建的正式存档位置 ID。

同一会话、同一类型和同一路径只能存在一个候选。不得持久化原始高频事件流、存档内容或注册表原始值。

## 11. 桥接接口

已提供以下窄接口：

| 接口 | 作用 |
| --- | --- |
| `preview_guided_save_detection` | 返回启动程序、可选范围、不可用项和隐私说明 |
| `start_guided_save_detection` | 重新验证范围、启动观察器并启动游戏 |
| `current_guided_save_detection` | 返回当前活动会话；没有活动会话时返回最近一个仍有未审核候选的完成会话 |
| `guided_save_detection_status` | 按会话 ID 返回最新状态，供前端在进入 `settling` 后持续读取完成结果 |
| `latest_guided_save_detection_for_game` | 返回指定游戏最近一个仍可继续审核的会话 |
| `mark_guided_save_saved` | 记录保存时间并启动约 3 秒自动收尾 |
| `stop_guided_save_detection` | 无保存标记时手动停止并分析 |
| `cancel_guided_save_detection` | 取消且不生成候选 |
| `list_guided_save_discoveries` | 读取已完成候选 |
| `accept_guided_save_discoveries` | 事务式接受所选候选并幂等创建正式位置 |
| `discard_guided_save_detection` | 明确丢弃尚未接受的旧结果 |
| `resolve_guided_close` | 处理“停止并分析后退出”“取消会话并退出”或“返回程序”的关闭选择 |

前端只在会话处于 `preparing`、`monitoring` 或 `settling` 时按约 1 秒间隔读取指定会话状态；进入终态后立即停止轮询，但保留完成会话用于审核。不新增 Python 主动执行任意 JavaScript 的回调通道。

## 12. 前端界面

在现有静态“查找存档”附近增加独立“引导式寻找”按钮。范围确认面板显示：

- 当前游戏和将启动的程序。
- 默认监控目录及勾选状态。
- 不可访问或不支持的目录。
- 定向注册表键。
- 只读取路径和元数据、不读取或修改存档内容的说明。

会话面板按状态显示：

- `preparing`：正在准备监控，游戏尚未启动。
- `monitoring`：游戏名、持续时间、变化数量、“我刚刚保存了”“停止并分析”和“取消会话”。
- `settling`：约 3 秒写盘等待与分析进度，禁止重复提交。
- `completed`：候选分组、证据、警告、接受和丢弃操作。
- 失败、取消和中断：可读原因及重新开始入口。

关闭详情后，主页顶部显示“正在为《游戏名》寻找存档”、阶段、持续时间和“返回向导”。状态栏不展示原始文件路径。

另一个游戏存在活动会话时，“引导式寻找”按钮保持可见但不可启动，并说明当前正在监控哪个游戏。

候选审核沿用现有存档建议卡片的视觉语言，但与静态结果分开。接受所选候选后刷新正式存档位置列表。

## 13. 错误处理与清理

- 至少一个有效范围才允许开始。
- 全部观察器启动成功后才能启动游戏；部分启动失败时关闭已经建立的观察器。
- 游戏启动失败时立即停止观察器，会话进入 `failed`，不生成候选。
- 监控期间单个目录失效时，其余目录继续；失效范围标记为不完整。
- 事件或队列溢出不终止会话，收尾时对受影响范围补扫。
- 进程跟踪失败时降级为手动结束，不声称游戏已退出。
- 取消必须在 `preparing`、`monitoring` 和 `settling` 中清理线程、句柄和内存事件。
- 候选分析使用事务：完整写入候选和完成状态，或会话失败且不留下半批候选。
- 接受多个候选使用单次事务；任一候选无效或不属于会话时整体拒绝。
- 正常关闭处理活动会话；异常退出由下一次启动标记 `interrupted`。
- 日志只记录会话 ID、阶段、计数和稳定错误码，不记录存档内容、注册表原始值或高容量事件列表。

所有失败、取消和中断都只影响本次会话。已有 `save_locations` 和实际存档文件保持原样。

## 14. V0.2 孤立存档发现

V0.2 目标是处理“游戏本体已经删除，但 AppData、Documents 或注册表中的存档仍在”。它是独立页面和独立扫描任务：

1. 用户选择默认已知目录或额外扫描目录。
2. 程序依据 Ludusavi、引擎目录结构和文件组合寻找候选。
3. 候选显示可能的游戏名、引擎、可信度和证据。
4. 用户可以关联现有游戏、创建仅存档卡片或忽略。

V0.2 只能提出可能关系，不能保证仅凭存档反推出准确游戏。通用的 `save01.dat`、`config.dat` 等缺少上下文时必须保持低可信度和未命名状态。

默认扫描不遍历所有磁盘，只检查：

- 可直接展开的 Ludusavi Windows 文件和注册表规则。
- `<winAppData>\RenPy` 下具有 Ren'Py 存档结构的目录。
- LocalLow 下有限深度的 Unity 公司/产品目录。
- Documents、Saved Games、AppData 中有强引擎特征的已知结构。
- 用户已经配置的自定义清单位置。

用户可以额外选择目录并设置 1～12 层深度，默认 6 层。扩展扫描必须可取消、显示进度、遵守全局排除项、跳过系统和缓存目录，并限制每个根目录的访问条目数。

孤立候选可以关联现有游戏、创建 `games.status = save_only` 的仅存档卡片，或保留为已忽略候选。任何操作都不能移动、重命名、删除或修改实际存档。V0.2 的最终范围、字段和审核交互必须在实施前单独设计确认。

## 15. 测试与验收

### 15.1 后端单元测试

- 会话全部合法状态转换和非法命令拒绝。
- 全程序单活动会话约束。
- 文件事件规范化、去重和时间窗口计算。
- 临时文件写入后重命名覆盖的合并。
- 目录聚合、噪声过滤和可信度分级。
- 无保存标记、溢出和截断时不默认勾选。
- 补扫深度、条目数和时间限制。
- 重解析点、UNC、越界路径和不可访问目录。
- 注册表快照只保留类型、长度和摘要。
- 取消、失败、重复命令和资源清理。
- 多候选接受的事务性和路径去重。
- 启动时把残留活动会话标记为 `interrupted`。

### 15.2 Windows 适配器测试

- 临时目录中的创建、修改、重命名和删除事件。
- 模拟 Windows 返回零字节及队列饱和。
- 停止时取消阻塞 I/O 并关闭全部句柄。
- 进程树正常退出、子进程延续和跟踪降级。
- 多次启动、停止后不遗留观察线程。

### 15.3 前端测试

- 范围确认及不可用目录提示。
- 各状态对应的按钮、文案和警告。
- 点击“我刚刚保存了”后自动进入等待和分析。
- 关闭详情后主页状态栏继续显示。
- 单会话期间其他游戏不能启动新会话。
- 候选分组、默认勾选、接受和丢弃。
- 正常退出时的活动会话确认。

### 15.4 集成与人工验收

合成端到端、Windows 适配器集成、硬上限和自动恢复测试已经实现。2026-08-16 已完成两轮真实游戏主流程、本机资源观察、多缩放、窄窗口和启动器脱离降级检查；目标 Windows 10/11 检查因当前没有可用测试设备而暂缓，条件具备后仍需在首发冻结前完成。

- 用测试程序模拟普通保存、自动保存和临时文件重命名保存。
- 同时制造缓存式噪声，真实存档候选仍排在前列。
- 正常情况下点击“我刚刚保存了”后，约 3 秒等待结束并在随后数秒内显示结果。
- 空闲监控不得周期性遍历 AppData，CPU 接近空闲。
- 队列、路径和候选数量始终受硬上限约束。
- 取消或启动失败后观察线程和目录句柄全部释放。
- 在 Windows 10/11 x64 验证普通游戏、启动器子进程和纯自动存档流程。
- 全部操作只读文件元数据，不修改、移动或删除测试存档。

2026-08-16 本机人工验收证据：

- 普通手动保存中，点击“我刚刚保存了”后约 3～4 秒显示结果；真实 `save` 目录是唯一中可信候选，另有 11 个低可信噪声候选。接受后正确写入 `source = dynamic` 的存档位置。
- 纯自动保存中，点击“停止并分析”后真实目录排在 13 个低可信候选首位；因为没有保存标记，所有候选均未默认勾选。接受后正确写入第二个动态存档位置。
- 关闭向导详情后全局状态栏持续显示且可返回；活动会话期间另一游戏不能再开始引导会话。
- 20 秒空闲监控样本中，CPU 时间增加 0.1094 秒（单核等效约 0.545%），读取 993,760 字节、写入 2,240 字节，21 个线程保持稳定；会话没有溢出、截断或跟踪降级。
- 普通“取消会话”后会话落为 `cancelled`、活动会话归零且无发现结果；GameShelf 线程和句柄保持稳定，已启动游戏按设计继续运行。
- 人工关闭验收发现“取消会话并退出”死锁：桥接线程持有 `GuidedSaveSessionService` 的重入锁并同步调用 `window.destroy()`，WinForms 关闭事件随后重入 `request_close()` 等待同一把锁。退出回调现已移到服务锁外执行；新增锁可重入回归测试后，精确人工复测确认窗口正常退出、会话为 `cancelled`、无活动会话或发现结果。
- “停止并分析后退出”人工复测中，会话无错误完成并生成 4 个候选，活动会话归零，GameShelf 自动退出，已启动游戏继续运行。
- 80%、100%、120% 三档界面缩放以及 120% 下最小 960×640 窗口均可正常使用；游戏详情和引导式范围预览没有按钮遮挡、文字重叠或异常横向滚动。
- 使用 `startup.exe` 启动实际游戏的样本能够保持可靠进程树跟踪，关闭实际游戏后后端自动完成分析，未错误显示跟踪降级。
- 随后使用离线合成样本复现真正的启动器脱离：GameShelf 启动的根进程通过 WMI 创建实际目标后约 2～3 秒退出，目标窗口继续运行且父进程不再属于原进程树；修复前最近 5 次会话均错误落为 `completed`、`process_tracking_degraded = 0` 且候选数为 0。根因是跟踪器只记录“是否曾观察到进程”，一旦观察过就把整棵树消失无条件视为可靠退出。
- 进程树跟踪器现记录首次观察时间并设置 5 秒启动保护期：保护期内整棵树消失时改为跟踪降级并保持会话监控；到达保护期边界后仍按可靠退出自动分析。引导面板同时显示“无法可靠判断游戏是否仍在运行”，并保留标记保存、停止分析和取消操作。小于 5 秒与恰好 5 秒边界、真实 Windows 子进程延续以及前端警告均有回归测试。同一离线样本人工复验中，目标窗口继续运行且响应正常，引导面板保持 `monitoring` 并显示降级提示及三个手动按钮；数据库持久化 `process_tracking_degraded = 1`，会话随后正常完成并生成 3 个候选，本机验收通过。
- 自动完成与用户命令竞态曾使前端残留旧 `monitoring` 状态：后端已完成时，命令返回 `guided_session_not_active`，通用失败处理却停止轮询且未刷新会话。前端现将该特定错误视为状态竞争并立即按会话 ID 重同步；回归测试先复现旧状态残留，再验证完成状态和候选自动加载，真实游戏精确复测通过。
- 修复后的完整门禁为 Python 555 项、前端 88 项，并通过 Ruff、mypy、前端类型检查、生产构建、补丁检查和源码冒烟。

完成实现后，在项目根目录激活统一的 Conda 开发环境并运行：

```powershell
conda activate .\.venv
python -m pytest
python -m ruff check src tests scripts
python -m mypy src scripts/update_ludusavi_snapshot.py scripts/benchmark_ludusavi_index.py
npm --prefix frontend run test:unit -- --run
npm --prefix frontend run type-check
npm --prefix frontend run build
python -m gameshelf --smoke-test
```

模块 07 定义的冻结冒烟与 Windows Sandbox 当前范围已经完成。额外目标 Windows 10/11 设备和特殊故障矩阵没有完整执行，不记为通过，作为后续可选兼容性复核。

## 16. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-12 | 初步设计动态文件变化检测和孤立存档扫描。 |
| 2026-08-13 | 从 V0.1 移出，拆分为 V0.2 引导式检测和 V0.3 孤立存档发现。 |
| 2026-08-13 | 删除施工级任务清单，保留后续版本边界、风险和验收方向。 |
| 2026-08-13 | 用户确认将引导式寻找存档重新纳入 V0.1 并作为发布门槛；孤立存档调整为 V0.2。 |
| 2026-08-15 | 确认 V0.1 最终设计：独立入口、向导启动、单活动会话、Win32 事件适配器、保存标记后约 3 秒自动分析、30 分钟防遗忘上限、受限补扫、事务式审核和开发期重新建库。 |
| 2026-08-15 | 完成 V0.1 实现：受限 Win32 观察器、进程树跟踪、单活动会话、候选评分与事务式审核、全局状态栏及关闭处理均已接入；自动验证为 Python 443 项、前端 86 项，并通过 Ruff、mypy、类型检查、生产构建与源码冒烟。真实游戏资源观察和交互验收保留为发布前待办。 |
| 2026-08-15 | 修复目录观察器停止竞态：当第一次 `CancelIoEx` 发生在下一次读取挂起之前时，在总停止超时内重试取消并等待线程退出；新增确定性回归测试并通过真实 Win32 集成测试。 |
| 2026-08-16 | 完成普通手动保存、纯自动保存、全局状态恢复、单会话限制、取消语义、空闲资源、80%/100%/120% 缩放及最小窄窗口的本机人工验收；两个真实目录均正确接受为动态存档位置。关闭验收同时发现并修复“取消会话并退出”持锁调用窗口销毁造成的 WinForms 重入死锁，新增回归测试并通过 Python 554 项、前端 86 项及完整质量门禁。剩余启动器跟踪降级和目标系统验收。 |
| 2026-08-16 | 修复自动完成与用户命令竞态造成的前端旧监控状态残留：`guided_session_not_active` 现在触发同一会话的立即状态重同步，不再停止轮询后保留无效按钮；新增前端回归测试并通过真实游戏精确复测。完整门禁更新为 Python 554 项、前端 87 项。启动器 `startup.exe` 样本已确认可可靠跟踪，真正脱离进程树的降级样本仍待验收。 |
| 2026-08-16 | 离线 WMI 重挂接样本复现启动器约 2～3 秒退出而目标窗口继续运行时会话错误自动完成。进程树跟踪现增加 5 秒启动保护期，期内整棵树消失改为降级并继续监控；引导面板补充降级提示且保留手动命令。Python 555 项、前端 88 项及完整门禁通过；同一样本复验确认目标仍运行、界面保持监控并显示提示，数据库记录降级，本机验收通过。 |
| 2026-08-16 | 用户确认当前没有可用于完整 Windows 10/11 验收的设备；目标系统检查暂缓但不视为通过或取消，继续保留为首发前待办。 |
| 2026-08-17 | 同步当前路线图结论：V0.1 引导式寻找存档当前范围已经完成；未执行的完整目标设备矩阵不记为通过，改列后续可选兼容性复核，不再阻塞 V0.1。 |
| 2026-08-18 | 为兼容自动存档后短过场动画，将保存标记时间加权调整为 5 秒内强加分、15 秒内中等加分；超过 15 秒的会话变化仍保留为候选，但不获得时间加分。边界回归测试覆盖恰好 5 秒、恰好 15 秒和超过 15 秒；完整门禁通过 Python 717 项、前端 142 项及 Ruff、mypy、类型检查、生产构建、补丁检查和源码冒烟。 |
