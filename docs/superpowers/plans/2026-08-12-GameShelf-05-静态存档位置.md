# GameShelf 存档位置管理

> 文档性质：当前有效的模块设计与验收基准；适用版本：V0.1 及后续迭代；最后更新：2026-08-13；状态：已实现。后续修改直接维护本文，不再为小修复单独新建设计文档或实施计划。

## 1. 模块目标

GameShelf 为每个游戏管理一个或多个存档位置，让用户可以查看、验证并直接打开这些位置。路径既可以由用户手动添加，也可以由静态规则给出候选建议。

本模块只负责“记录存档在哪里”，不读取、复制或修改存档内容。

### V0.1 核心范围

- 手动添加目录、文件、通配符或注册表形式的存档位置。
- 一个游戏可以保存多个存档位置。
- 显示路径是否存在、匹配文件数和最近验证时间。
- 打开存档目录，或在资源管理器中定位存档文件。
- 删除已记录的位置。
- 用户明确点击“查找存档”后，使用本地静态规则提供候选位置。

### 延后范围

- V0.2：启动游戏前后进行引导式文件变化对比，辅助发现未知存档。
- V0.3：扫描孤立存档，并尝试反推已经删除的游戏。
- V0.4 或更晚：存档备份、恢复、同步和版本管理。

这些延后功能不能成为 V0.1 发布的阻塞项。

## 2. 用户操作流程

### 2.1 手动管理

1. 用户在游戏详情中展开“存档位置”。
2. 选择目录、文件、通配符或注册表位置；目录和文件使用系统选择器。
3. GameShelf 将绝对路径转换成可迁移的路径模板后保存。
4. 已保存位置显示类型、来源、实际路径和存在状态。
5. 用户可以打开、重新验证或移除该位置。

手动添加的位置视为已确认数据，可信度为 `1.0`，静态建议不能覆盖它。

### 2.2 静态查找

1. 打开游戏详情时不自动匹配规则。
2. 展开存档区域时也不自动匹配规则。
3. 只有用户点击“查找存档”后，才加载自定义清单、Ludusavi 清单和引擎提示。
4. 候选项按“高可信建议”“可能的位置”“实验性引擎建议”分组展示。
5. 用户勾选候选项并确认后，候选项才写入数据库。

静态查找是可选辅助。即使规则缺失、损坏或匹配失败，手动管理仍须正常工作。

## 3. 存档位置模型

每条已保存位置包含：

| 字段 | 含义 |
| --- | --- |
| `id` | 稳定 UUID |
| `game_id` | 所属游戏 |
| `kind` | `directory`、`file`、`glob` 或 `registry` |
| `path_template` | 用于迁移和解析的路径模板 |
| `display_path` | 当前电脑上的可读路径 |
| `path_key` | Windows 规范化去重键 |
| `source` | `manual`、`dynamic`、`ludusavi`、`engine` 或 `legacy_scan` |
| `confidence` | 来源给出的可信度 |
| `evidence` | 规则来源和匹配依据 |
| `confirmed` | 是否已由用户确认 |
| `enabled` | 是否启用 |
| `last_verified_at` | 最近验证时间 |

已保存位置按 `game_id + kind + path_key` 去重。静态候选在合并时还会规范化普通文件系统路径的类型，避免同一实际路径因来源表达不同而重复显示；注册表位置单独处理。

## 4. 便携路径模板

程序不直接持久化任意绝对文件系统路径，而是优先折叠成以下模板：

| 令牌 | Windows 位置 |
| --- | --- |
| `<game>` | 当前游戏安装目录 |
| `<home>` | 用户主目录 |
| `<winAppData>` | Roaming AppData |
| `<winLocalAppData>` | Local AppData |
| `<winLocalAppDataLow>` | LocalLow |
| `<winDocuments>` | 文档 |
| `<winSavedGames>` | Saved Games |
| `<winProgramData>` | ProgramData |
| `<winPublic>` | 公共用户目录 |
| `<winDir>` | Windows 目录 |

折叠时选择匹配到的最长根路径，例如 Unity 存档优先表示为 `<winLocalAppDataLow>\厂商\游戏名`。展开时拒绝未知令牌、绝对后缀、UNC、额外嵌套令牌和包含 `..` 的越界路径。

不在这些便携根目录内的路径不自动保存；用户需选择可安全表示的位置。

## 5. 位置类型与打开规则

- `directory`：目录必须存在；点击后在资源管理器中打开。
- `file`：文件必须存在；点击后在资源管理器中定位该文件。
- `glob`：显示当前匹配数量；点击后打开最近的现有父目录。
- `registry`：使用 `HKEY_CURRENT_USER` 或 `HKEY_LOCAL_MACHINE`；手动添加的键必须存在，接受规则建议时需要额外确认，未确认的位置不能打开。

验证操作只更新存在状态、匹配数量和验证时间，不创建目录或文件。通配符匹配数量设置上限，避免异常规则导致无界遍历。

## 6. 静态建议来源

建议来源按以下顺序执行并合并：

1. 用户自定义清单。
2. GameShelf 当前有效的 Ludusavi 官方清单。
3. 基于已识别游戏引擎的保守提示。

相同位置由多个来源命中时只显示一次，同时保留所有来源证据，并采用其中更强的分组和最高可信度。数据库中已经存在的位置不再作为建议显示。

### 6.1 Ludusavi 匹配

GameShelf 只解析与 Windows 有关的 Ludusavi 数据子集。条目匹配会综合使用游戏标题、安装目录名、可执行文件名和清单别名；规则条件作为候选位置的适用证据展示。

- 确认匹配进入“高可信建议”。
- 模糊匹配进入“可能的位置”。
- 高可信、类别为存档且非注册表的建议可以默认勾选。
- 注册表建议永不默认勾选。
- 建议在用户接受前不写入数据库。

### 6.2 引擎提示

目前引擎提示覆盖：

- Ren'Py：读取脚本中明确的 `config.save_directory` 字符串。
- Unity：从 `*_Data/app.info` 取得公司名和产品名，建议 LocalLow 目录及 PlayerPrefs 注册表键。
- RPG Maker 2000、XP、VX、VX Ace、MV、MZ：只在找到对应世代的现有存档文件时提出建议。
- WOLF RPG Editor：在受限深度和数量内查找已有存档目录或存档文件。
- KiriKiri：只在 `save` 或 `savedata` 目录中发现相应存档文件后提出建议。
- NScripter：只在游戏目录发现 `save*.dat`、`envdata` 或 `kidoku.dat` 后提出建议。

引擎提示必须有本机文件或可靠元数据作为证据，不能只凭“这个引擎通常使用某个目录”就声称已经找到存档。标记为实验性的引擎规则只能进入“实验性引擎建议”。

### 6.3 自定义清单

自定义清单位于程序旁的 `data/manifests/custom`，格式与 GameShelf 支持的 Ludusavi 子集兼容。

- 只读取 `.yaml` 和 `.yml`。
- 单个文件最大 8 MiB。
- 最多读取 100 个文件。
- 单个自定义清单损坏时跳过该文件并显示错误，不影响其他清单和手动位置。

## 7. Ludusavi 清单生命周期

### 7.1 三层文件

| 层级 | 位置 | 用途 |
| --- | --- | --- |
| 内置种子 | `resources/manifests/ludusavi` | 首次运行或恢复失败时使用 |
| 当前有效清单 | `data/manifests/ludusavi` | 实际匹配读取 |
| 历史备份 | `data/manifests/ludusavi/previous` | 保留最近两组有效快照 |

清单和 `manifest-meta.json` 必须作为一组处理。元数据记录来源 URL、下载时间、ETag、SHA-256 和可选的上游提交号。

### 7.2 启动与状态查询

- 程序启动时不会联网更新。
- 官方清单状态查询只检查文件组是否完整、元数据是否合法以及 SHA-256 是否一致，不解析大型官方 YAML；自定义清单仍会单独解析，以便在状态区域报告具体文件错误。
- 首次缺少活动清单时，从有效备份恢复；没有有效备份时再复制内置种子。
- 仓库内置 YAML 固定使用 LF。仅当 CRLF 内容规范化为 LF 后与元数据哈希完全一致时，才允许受控修复换行。

### 7.3 手动更新

只有用户点击“检查并更新清单”后才启动后台更新任务。过程依次显示连接、下载、校验和替换状态。

更新必须满足：

- 更新地址仅允许 HTTPS。
- 网络超时为 30 秒。
- 使用 ETag 进行条件请求。
- 响应上限为 64 MiB，并采用流式下载。
- 下载落盘后再次校验 SHA-256。
- 完整解析并验证新 YAML 后，才允许替换活动清单。
- 替换前备份旧的有效文件组，替换使用原子操作。
- 下载、校验或替换失败时继续使用最后一次有效清单，并向用户显示本次失败原因。
- 官方清单不可用时，手动位置、自定义清单和引擎提示仍然可用。

## 8. 性能约束

- 打开游戏详情不得触发大型清单解析。
- 展开存档区域不得触发大型清单解析。
- 官方清单状态查询不得解析大型官方 YAML。
- 静态匹配只在点击“查找存档”后执行。
- 同一运行周期内可复用已解析的官方清单，清单成功更新后必须使缓存失效。
- 引擎目录探测必须限制深度、文件数量和单文件读取大小。

已知后续优化：当前首次“查找存档”仍可能因大型 Ludusavi YAML 的完整解析而等待较久；后续应优化清单加载与索引缓存，但这不影响 V0.1 的手动存档位置功能。

## 9. 故障与安全边界

- 无效路径模板不能落库。
- 注册表只允许受支持的根键，并要求显式确认。
- 自动建议不能覆盖或删除手动位置。
- 规则读取不得修改游戏目录或存档文件。
- 程序不在后台静默联网。
- 清单更新失败不能删除最后一次有效快照。
- 所有后台任务失败都通过稳定错误结构返回，界面不得无限显示加载状态。

## 10. 代码职责

| 区域 | 主要职责 |
| --- | --- |
| `src/gameshelf/saves/templates.py` | 路径模板折叠、展开和越界保护 |
| `src/gameshelf/saves/service.py` | 已确认位置的增删、验证、打开和去重 |
| `src/gameshelf/saves/static_discovery.py` | 合并三类静态建议并生成稳定建议 ID |
| `src/gameshelf/saves/ludusavi_parser.py` | 防御性解析 Ludusavi 子集 |
| `src/gameshelf/saves/ludusavi_matcher.py` | 游戏与清单条目匹配 |
| `src/gameshelf/saves/ludusavi_provider.py` | 快照校验、恢复、更新和缓存来源 |
| `src/gameshelf/saves/engine_hints.py` | 有证据约束的引擎存档提示 |
| `src/gameshelf/saves/custom_manifest_provider.py` | 隔离加载用户清单 |
| `src/gameshelf/bridge/api.py` | 向前端提供稳定 API 和后台任务状态 |
| `frontend/src/features/saves` | 存档列表、添加对话框、建议审核和规则设置界面 |

## 11. 验收基准

以下行为必须持续成立：

- 一个游戏可保存多个目录和文件位置，并在重启后保留。
- 相同位置不会因路径大小写或分隔符差异重复保存。
- 游戏目录迁移后，`<game>` 模板在验证和打开时仍能解析到新的安装目录。
- 不存在的位置显示为失效，但不会被自动删除。
- 打开目录、定位文件和打开通配符父目录符合各自规则。
- 注册表建议未确认时不能接受或打开。
- 进入详情页和展开存档区域时不调用静态建议接口。
- 点击“查找存档”后才显示三类来源合并的建议与证据。
- 官方清单损坏或更新失败时，最后一次有效清单和手动管理保持可用。
- 状态查询能快速结束，不长时间停留在“正在读取清单状态”。
- 更新任务无论成功、无需更新、内容无效或网络失败，界面都会结束加载并显示结果。

## 12. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-12 | 完成多存档位置、便携模板、Ludusavi、自定义清单、引擎提示和审核界面的首轮实现。 |
| 2026-08-13 | 明确 V0.1 以手动管理为核心，动态发现和孤立存档后移。 |
| 2026-08-13 | 修复 Ludusavi 快照换行、校验、备份恢复和失败降级机制。 |
| 2026-08-13 | 清单状态改为元数据与哈希快速校验，不再完整解析 YAML。 |
| 2026-08-13 | 静态建议改为用户点击“查找存档”后按需执行。 |

## 13. 本次归并来源

本文已经吸收以下零散文档中的当前有效结论：

- [`2026-08-13-Ludusavi清单更新修复设计.md`](../../历史归档/2026-08/2026-08-13-Ludusavi清单更新修复设计.md)
- [`2026-08-13-Ludusavi清单更新修复实施计划.md`](../../历史归档/2026-08/2026-08-13-Ludusavi清单更新修复实施计划.md)
- [`2026-08-13-Ludusavi清单状态快速读取修复.md`](../../历史归档/2026-08/2026-08-13-Ludusavi清单状态快速读取修复.md)
- [`2026-08-13-静态存档建议按需触发修复.md`](../../历史归档/2026-08/2026-08-13-静态存档建议按需触发修复.md)

上述文件已经移入 `docs/历史归档/2026-08`，只用于追溯，不再作为当前开发依据。
