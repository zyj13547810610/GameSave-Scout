# GameShelf 游戏引擎识别

> 文档性质：当前有效的模块设计与验收基准；适用版本：V0.1 及后续迭代；最后更新：2026-08-25；状态：V0.1 第一批识别器、首发识别补强、损坏声明式规则降级、V0.3.0 内置规则扩展、V0.3.1 自定义规则编辑器和 V0.3.2 第二批规则扩展均已完成；V0.3.1 真实窗口人工验收已通过，真实游戏规则矩阵继续后置复测。

## 1. 模块目标

GameShelf 在扫描游戏目录时，以只读方式推测游戏引擎，并向用户展示可信度和证据。引擎识别用于分类、筛选、解释和辅助存档定位，不决定一个目录能否进入游戏库。

核心原则：

- 有 EXE 或被直接子目录模式发现的文件夹都可以成为游戏，即使引擎未知。
- 不运行游戏、不加载 DLL、不解包商业资源、不注入进程。
- 多个独立证据优于单个通用文件名。
- 无法确定时宁可显示“未知引擎”，不强行给出结果。
- 用户手动选择始终高于自动结果，并在重新扫描后保留。

## 2. 检测输入与时机

扫描流程先确定候选游戏目录，再推荐主 EXE，最后调用引擎检测。输入包括：

- 游戏目录。
- 当前用于检测的主 EXE；有效手动选择优先，否则使用自动推荐。
- 目录内有限的文件和文件夹结构。
- 小段文本、文件头/尾魔数和有限 PE 版本信息。

检测器不读取存档内容，也不依赖游戏平台元数据。

## 3. 检测器架构

### 3.1 两阶段检测

每个检测器实现：

1. `cheap_probe`：使用少量路径存在性快速判断是否值得深入检查。
2. `inspect`：在受限读取范围内生成 `EngineMatch`。

每个结果包含引擎 ID、可选变体、可信度、证据列表、规则版本和是否为实验性。

### 3.2 结果选择

- 正式识别器最低可信度为 0.70。
- 实验识别器最低可信度为 0.80。
- 最多保留可信度最高的 3 个候选。
- 前两名差值小于 0.08 时标记为歧义，不自动采用第一名。
- 单个检测器异常只生成诊断证据，不中断整个目录扫描。

自动检测值写入 `detected_engine_id` 和 `detected_engine_variant`；界面采用值写入 `engine_id` 和 `engine_variant`。手动设置后 `engine_is_manual = true`，后续扫描仍更新检测值，但不覆盖采用值。

## 4. 当前正式识别范围

### 4.1 专用 Python 检测器

| 引擎 | 主要证据 |
| --- | --- |
| RPG Maker 2000/2003 | `RPG_RT.exe` 与地图/数据库配对 |
| RPG Maker XP | `Game.ini` 的 RGSS1 与对应归档/运行时 |
| RPG Maker VX | `Game.ini` 的 RGSS2 与对应归档/运行时 |
| RPG Maker VX Ace | `Game.ini` 的 RGSS3 与 `.rgss3a` 归档或 `Data/Scripts.rvdata2` 解包结构 |
| RPG Maker MV | `rpg_core.js` 与 `System.json` 配对 |
| RPG Maker MZ | `rmmz_core.js` 与 `System.json` 配对 |
| MKXP-Z | MKXP 配置与可执行文件配对 |
| RGU | RGU 可执行文件与 RGSS 归档配对 |
| Ren'Py | `game` 脚本与 Ren'Py/Lib 运行时配对 |
| Unity | `UnityPlayer.dll` 与所选 EXE 同名 `_Data/globalgamemanagers` 配对 |
| Unreal Engine | `Engine/Binaries`、项目 `Binaries` 与 Bootstrap 或 Shipping 入口配对 |
| WOLF RPG Editor | `Game.exe` 与明文或加密 WOLF 游戏数据配对 |
| SMILE GAME BUILDER | PE 产品信息与专属运行时文件配对 |
| RPG Developer Bakin | PE 产品信息与 Bakin 数据文件配对 |
| Visual Novel Maker | PE 产品信息与专属脚本数据配对 |

Unity 检测以有效主 EXE 所在目录为运行时根，因此兼容“候选游戏文件夹外层再套一层、Unity 本体位于内层”的结构。

### 4.2 V0.1 首发识别补强（已完成）

以下工作已经完成，并继续作为 V0.1 回归门槛：

- **RPG Maker VX Ace 解包结构**：除 `Game.ini` 的 RGSS3 与 `.rgss3a` 归档外，还要接受 `Game.ini` 的 RGSS3 与 `Data/Scripts.rvdata2` 等已解包数据结构。GameShelf 只识别现有文件，不负责解包或修改游戏资源。
- **RPG Maker MV 双布局回归**：继续兼容根目录下的 `js/rpg_core.js + data/System.json`，以及嵌套 `www/js/rpg_core.js + www/data/System.json`；两种布局都必须有独立回归测试。
- **Artemis 可变名称 PFS**：不再固定依赖 `data.pfs`，改为检查游戏目录内任意名称的根级 `*.pfs` 文件头；仍需与 MJA 等另一项独立证据配对，避免仅凭扩展名误判。
- **Unreal Engine 正式检测**：新增正式引擎选项和专用检测器，复用主程序排序阶段已经识别的 Unreal 运行时结构。至少覆盖 `Engine/Binaries`、非 `Engine` 项目目录的 `Binaries`、`BootstrapPackagedGame` 启动器和 `*-Win64-Shipping.exe`；`UnrealCEFSubProcess.exe` 等辅助程序不能成为正面引擎证据。
- **重扫数据更新与手动保护**：每次成功重扫都更新 `detected_engine_id`、`detected_engine_variant`、可信度、证据和规则版本；`engine_is_manual = true` 时不得覆盖用户采用的 `engine_id` 和 `engine_variant`。检测器异常时继续保留最后一次有效自动结果。

这些项目全部使用合成最小目录测试，不提交用户本地商业游戏文件；扫描集成测试同时验证数据库自动结果刷新与手动覆盖保护。

### 4.3 正式声明式规则

| 引擎 | 典型证据 |
| --- | --- |
| TyranoScript | `Config.tjs` 与 Tyrano 运行时 |
| KiriKiri 2/Z | XP3 魔数与 TJS/KS 文件 |
| ChoiceScript | `startup.txt` 与统计场景文件 |
| SRPG Studio | `data.dts` 与 `runtime.rts` |
| Pixel Game Maker MV | `AGtk.js` 与 `package.json` |
| Artemis | PFS 与 MJA 文件头 |
| RealLive | `Gameexe.ini` 与 `seen.txt` 魔数 |
| SiglusEngine | PE 产品信息与 `Scene.pck`、`Scene.chs` 或 `Scene.gbk` |
| BGI/Ethornell | `data.arc` 文件头及 BSS/ARC 配套文件 |
| CatSystem2 | CsPack2 文件头与 CST 文件 |
| YU-RIS | YPF 文件头与 YBN 文件 |
| NScripter/ONScripter | `nscript.dat` 与 NSA 归档 |

SiglusEngine 同时接受中文汉化版常见的 `Scene.chs` 和 `Scene.gbk`，不再强制要求 `Scene.pck`。

## 5. 当前实验识别范围

V0.3.0 校准后，SoftPal/AmuseCraft 仍是当前唯一保持实验状态的内置引擎规则。QLIE、Majiro、Malie、ShiinaRio、Entis/ERI/NOA 和 Nitroplus 已依据公开格式资料、合成正向样本及相似结构负向样本转为正式规则。

实验规则需要更高阈值并在界面明确标识。后续只有在获得合法、最小化的合成样本或公开格式资料并完成误报测试后，才能转为正式规则；V0.3.2 从 GARbro 直接特征候选中筛出的短签名或加密头变体也必须先保持实验状态。

## 6. 声明式规则格式

当前内置规则位于 `resources/rules/builtin/engines.yaml`，结构契约位于 `resources/rules/schemas/engines.schema.json`；用户规则一条一个 YAML，保存到 `data/rules/user/engines`。每条规则包含：

- 稳定引擎 ID 和用户可读标签。
- 可选变体。
- 是否实验性。
- 可信度阈值。
- `all`、`any` 和 `negative` 证据组。

当前支持的安全操作包括：

- 路径是否存在。
- 受限 glob 是否命中。
- 受限 glob 命中文件后，在固定偏移校验文件魔数。
- 小文本是否包含特征串。
- 固定偏移文件魔数。
- 文件头尾受限范围内的特征串。
- PE 版本字段包含指定文本。

规则路径必须是安全相对路径。读取器限制文件大小、读取字节数和 glob 数量，不能通过 YAML 请求执行代码或访问游戏目录外路径。

### 6.1 V0.3 共享规则基础层

V0.3 继续把引擎识别规则和存档位置规则维护为两套独立目录与结构，不把不同用途强行合并成一种大型 YAML；两者共用规则元数据、来源、校验、诊断和冲突处理机制。共享字段至少包括稳定规则 ID、规则类型、内置或用户来源、正式或实验状态、规则版本、公开依据、优先级和启用状态。

- 内置规则使用 `builtin` 命名空间并随 GameShelf 版本发布，不提供在线规则更新。
- V0.3.x 用户规则使用 `user` 命名空间并单独保存在程序旁 `data/rules/user/engines`，不得修改发布包内置文件。
- 内置和用户规则不得静默复用同一个稳定 ID；用户需要调整内置规则时，应复制成新的用户规则并以更高优先级参与候选排序。
- 正式与实验规则遵守同一安全读取边界；状态只影响阈值、排序和界面标识，不能放宽路径、读取或执行限制。
- 本轮规则扩展不新增持久业务表，也不要求数据库迁移。

V0.3.x 继续以文件为唯一事实来源。内置与用户规则连同 `data/rules/settings.json` 中的本地启停设置被编译为不可变快照；每次游戏分析在开始时捕获快照。编辑规则后只影响下一次分析或完整扫描，不自动重跑当前游戏库，也不覆盖人工选择的引擎。

## 7. 主程序与检测联动

引擎检测准确度依赖主 EXE 选择。当前联动规则：

- 自动扫描时先进行主程序排序。
- 用户手动选择的 EXE 如果仍存在且位于游戏目录内，检测优先使用该 EXE。
- 手动 EXE 无效时安全回退到自动建议，但不静默清除手动设置。
- 确认游戏移动时保留有效手动主程序及其架构。
- 检测器失败时保留上次有效引擎状态，不能用一次异常清空结果。

主程序排序的具体规则见模块 02。

## 8. 证据与手动覆盖

详情界面同时显示：

- 当前采用的引擎及其是否手动设置。
- 最近自动检测的引擎、变体和可信度。
- 支持结论的文件或元数据证据。
- 歧义候选和检测器诊断。

用户可以：

- 从已知引擎列表选择。
- 选择“未知引擎”。
- 输入最多 80 字符的自定义引擎名称。
- 清除手动值，重新采用最近自动检测结果。

手动选择只改变游戏库元数据，不改动规则文件。

## 9. 与 MTool 的关系

本项目曾使用用户本地的 MTool 安装目录和其公开支持范围作为调研线索，用于核对引擎名称和常见格式；GameShelf 不复制 MTool 的程序代码、私有数据库或运行时行为。

两者交互目标不同：

- MTool 以用户拖入目标 EXE 为入口，先判断它是否属于受支持引擎，再决定是否启动并提供翻译能力。
- GameShelf 先扫描目录建立游戏库；引擎未知也允许收录和启动，只是不能得到对应的引擎标签或存档提示。
- GameShelf 当前全部使用静态只读证据，不通过启动游戏来判断引擎。

因此，“MTool 不支持就不启动”的限制不适用于 GameShelf。

## 10. 安全与性能约束

- 不调用发现的 EXE、DLL 或脚本。
- 不完整解压归档，不扫描用户目录之外的内容。
- 不跟随符号链接和重解析点越界。
- 文本、二进制和 PE 读取必须有大小上限。
- glob 数量和规则数量必须有上限。
- 单个检测器运行异常，以及已存在但无法解析或校验的声明式规则错误，只影响对应检测器；内置专用检测器、游戏库扫描和应用启动必须继续可用，并记录可诊断错误。
- 商业游戏样本不得提交到开源仓库；测试使用合成夹具和公开格式信息。

## 11. 代码职责

| 区域 | 主要职责 |
| --- | --- |
| `src/gameshelf/engines/base.py` | 检测器协议和上下文 |
| `src/gameshelf/engines/registry.py` | 执行、阈值、歧义和候选排序 |
| `src/gameshelf/engines/detectors` | 复杂专用检测器 |
| `src/gameshelf/engines/rule_detector.py` | 声明式规则执行 |
| `src/gameshelf/engines/rule_schema.py` | YAML 结构校验和安全限制 |
| `src/gameshelf/engines/bounded_reader.py` | 受限文件读取 |
| `src/gameshelf/engines/service.py` | 组合检测器、标签与用户选项 |
| `resources/rules/builtin/engines.yaml` | 可审查、可版本化的只读内置引擎规则 |
| `data/rules/user/engines/*.yaml` | 一条一个文件的用户引擎规则 |
| `src/gameshelf/rules` | 共享文件仓储、快照、冲突、安全校验和诊断 |
| `frontend/src/features/engines` | 结果、证据和手动覆盖界面 |

## 12. 验收基准

- 未知引擎游戏仍能进入游戏库并设置主 EXE。
- 每个正式检测结果都至少包含一项可解释证据。
- 单一通用文件名不足以给出高可信结论。
- 前两名接近时不自动采用任意一方。
- 单个检测器异常不导致扫描任务整体失败。
- 手动引擎和手动主 EXE在重新扫描、根目录重映射和确认移动后保留。
- Unity 内层运行时、Siglus 汉化场景文件和常见辅助程序目录有回归测试。
- VX Ace 归档与解包结构、MV 根目录与 `www` 嵌套结构都有回归测试。
- Artemis 任意名称根级 `.pfs` 只有在文件头和独立配套证据同时命中时才被识别。
- Unreal 正式检测覆盖 Bootstrap 与 Shipping 两类入口，并排除引擎内部辅助程序。
- 成功重扫会刷新自动识别字段；手动引擎采用值在重扫和检测器异常后保持不变。
- 声明式规则不能读取游戏目录之外的路径，也不能执行文件。
- 实验规则需要更高阈值并在界面中可区分。
- `engines.yaml` 存在但内容损坏时，程序仍能启动并保留内置专用检测器；声明式检测器被禁用，日志明确记录规则加载失败。
- V0.3.x 内置规则只能查看、测试、本地禁用或复制，不能直接写回随包 YAML；用户规则编辑失败时旧快照继续可用。
- 新建或复制的用户引擎规则默认实验；成功只读测试后可由用户标记“用户规则 · 已验证”，修改匹配或证据条件后自动恢复为实验。
- 同一游戏由多条规则命中时继续按证据分数、置信度与优先级排序并保留歧义；用户规则和较高优先级不能覆盖有效的人工引擎采用值。

### 12.1 2026-08-16 损坏规则验收与修复设计

- 初次在独立轻量版冻结副本中验收时，干净规则的发布清单和 JSON smoke 均通过；将 `engines.yaml` 替换为无法解析的 YAML 后，冻结进程退出码为 1，错误能够指出 YAML 解析位置，但 `applicationBootstrap` 未完成，整个应用未启动。因此本项当时未通过，并触发本次修复。
- 根因边界已经定位：`build_application()` 启动期间直接调用 `EngineDetectionService.from_rules_file()`，后者同步调用 `load_engine_rules()`；`RuleSchemaError` 未在检测器组合边界转换为“禁用声明式检测器并继续”，而是直接中止应用构建。
- 测试后已恢复原规则，恢复后的冻结 smoke 再次退出 0。
- 已确认的修复边界：应用构建层只在规则路径仍是现有普通文件、但内容无法解析或结构校验失败时捕获 `RuleSchemaError`，向程序旁 `data/logs/gameshelf.log` 写入 WARNING，然后仅使用内置专用检测器继续启动。V0.1 不增加前端横幅或原生弹窗，避免可恢复的识别能力降级阻断用户。
- 规则文件缺失或不再是普通文件仍属于发布完整性故障，必须保留启动失败；命令行诊断工具继续使用严格规则加载，不改变其失败语义。
- 修复已在应用构建层实现：损坏 YAML 和结构错误写入 WARNING 后使用 `EngineDetectionService.builtins_only()` 继续启动；规则加载前移到数据库迁移和后台写线程之前，缺失文件仍严格失败且不再产生半初始化数据库。`EngineDetectionService.from_rules_file()` 保持严格语义。
- 测试先行证据为新增 3 个用例先全部失败、实现后全部通过；应用与引擎相关回归为 90 项，全量 Python 为 558 项，前端为 88 项，并通过 Ruff、mypy、前端类型检查、生产构建和源码 smoke。
- 随后已从干净提交 `6b29a8f1785dd123904e8e39dc9dadfa3bf89b9f` 正式重建双版本六个产物。在隔离的轻量版冻结副本中将 `engines.yaml` 替换为损坏 YAML 后，JSON smoke 退出码为 0，`applicationBootstrap` 与 `engineRules` 均为 `true`；程序旁普通日志记录“声明式引擎规则加载失败，已仅启用内置检测器”，且未生成 `startup-error.log`。恢复原规则后冻结 smoke 再次退出 0，本修复的源码与冻结版验收均已完成。

## 13. V0.3 内置规则扩展与自定义编辑器

### 13.1 V0.3.0 首批范围

V0.3.0 已按“同人游戏、Galgame 常见引擎优先，其次补充通用引擎”的顺序完成以下 10 个重点项：

- QLIE、Majiro、Malie、ShiinaRio、Entis/ERI/NOA 和 Nitroplus 经证据台账、合成正反样本与规则回归校准后转为正式。
- SoftPal/AmuseCraft 仍主要依赖短魔数且缺少足够真实样本，明确保持实验，不按计划数量强行转正。
- LiveMaker、CMVS/CVNS 和 Godot 已作为正式规则加入，并具备公开依据、合成正向样本和相似结构负向样本。

规则仍不得按计划数量强行转正式。只有稳定文件魔数、目录组合、可解析元数据等证据充分，并且具有正向与相似结构负向回归时才可转正式；只依赖常见文件名、误报风险仍高或公开依据不足的规则继续保持实验。

自动测试使用公开格式资料与合法的最小合成样本。当前电脑缺少的真实游戏仍保留为人工验收矩阵，在具备相应游戏的电脑上复测；仓库不加入商业游戏资源或由其截取的受版权保护载荷。开发者可使用 `detect_directory.py --sanitized` 生成受限深度、相对路径和短文件头的脱敏诊断报告，诊断过程不越过游戏目录或重解析点。

### 13.2 V0.3.1 自定义规则编辑器（已实现并通过人工验收）

“规则管理”作为与游戏库、批量存档同级的独立工作台，顶部提供“引擎规则”“存档规则”“Ludusavi”三个标签。正常宽度使用左侧独立滚动规则列表和右侧独立滚动详情/编辑器，搜索、筛选和底部主要操作固定；110%/120% 或窗口过窄时改为先显示全宽列表、选中后进入全宽详情，并提供返回按钮，不把两列压成不可操作的细栏。

引擎规则标签遵循：

- 内置规则只读，可查看 ID、状态、来源、证据和 YAML 预览，也可本地启停、复制为新用户规则或选择一个游戏执行只读测试。
- 用户规则支持新建、修改、启停、复制、导入、导出和删除；显示名称可重复，但稳定 ID 全局唯一，不能复用内置 ID。
- 引导式表单依次编辑规则类型、基本信息、匹配条件和证据；第一版不开放任意 YAML 文本编辑，更不允许代码或脚本。
- 引擎规则继续使用现有受限 `all`、`any`、`negative` 证据操作。保存前执行 schema、安全、ID 冲突、路径边界和完整目录编译，失败时不写入文件。
- 只读测试报告命中证据、排除证据、分数、置信度和最终结论，不修改游戏记录、自动引擎结果、人工采用值或分析缓存。
- 新建和从内置复制的规则默认实验。成功测试后用户可以手动标记已验证，但始终显示为“用户规则 · 已验证”，不冒充 GameShelf 官方验证；修改匹配、证据或路径会自动恢复为实验，修改说明、依据或启停状态不会。
- 编辑器自身保存后热交换规则快照；程序外修改文件需要点击“刷新规则”或重启。已运行的扫描继续使用启动时快照，新任务使用新快照。

离开有未保存修改的编辑器前必须确认。新建和编辑直接使用右侧工作区，不再套大型弹窗；只有删除和覆盖导入等危险操作使用确认弹窗。弹窗位于所有选择框之上，非危险弹窗允许点击遮罩关闭。

### 13.3 共享导入、导出与冲突处理

引擎和存档用户规则共用最小文件交换流程：

- 一个 YAML 文件对应一条规则；导入对话框可一次选择一个或多个 `.yaml`/`.yml`，但第一版不支持 ZIP 规则包、在线市场或自动同步。
- 导入先展示规则类型、名称、稳定 ID、来源、实验/验证状态和完整诊断，用户确认后才写入。
- 与现有用户规则 ID 冲突时可选择生成新 ID 或覆盖该用户规则；与内置 ID 冲突时只能生成新的 `user` ID，永远不能覆盖随包规则。
- 外部规则无论声明什么来源，进入后都标记为用户规则。合法导入可以保留“已验证”状态，但仍显示“用户规则 · 已验证”，不冒充内置或 GameShelf 官方验证。
- 覆盖用户规则和删除规则必须再次确认；任何校验、写入或完整快照编译失败都保留原文件和当前编辑上下文。
- 导出只包含该规则的可移植声明，不包含本地游戏安装路径、测试结果、数据库内容、诊断缓存或其他用户数据。

多条规则可以使用相同显示名称，但稳定 ID 全局唯一。多条引擎规则命中同一游戏时仍保留全部候选和证据，优先级只用于排序与平分处理；不能用用户规则静默覆盖人工引擎采用值。

### 13.4 V0.3.2 GARbro 直接特征引擎规则（已实现）

V0.3.2 继续优先覆盖同人游戏和 Galgame 常见引擎，并且不把“暂缓”候选混入实施表。本节列出的规则同时满足以下条件并已实现：GARbro 的具体处理器明确把格式归入引擎/系统运行时；GameShelf 可以用现有 `path_exists`、`glob_exists`、`magic_at`、`glob_magic_at` 或固定路径 `magic_from_end` 表达；特征位于固定文件名、受限扩展名或固定偏移，不需要解密、解包或解析动态索引。

计划作为正式规则加入的 45 个强特征族如下：

| 引擎/系统族 | 直接证据 | GARbro 处理器 |
| --- | --- | --- |
| ActiveSoft ADPACK32/A98SYS | `*.pak` 同时命中 `ADPA` 与偏移 4 的 `CK32` | `ArcFormats/ActiveSoft/ArcADPACK.cs` |
| ADVScripter | `*.pak` 命中 `MD002`，并在偏移 `0x21` 命中 `00V` | `ArcFormats/AdvScripter/ArcPAK.cs` |
| AdvSys_T | `*.fpk` 命中 `MFWY` | `ArcFormats/AdvSys/ArcAdvSysT.cs` |
| AliceSoft System 4 | `*.afa` 同时命中 `AFAH` 与偏移 8 的 `AlicArch` | `ArcFormats/AliceSoft/ArcAFA.cs` |
| Aoi | `*.box` 命中 `AOIB`、`AOIM` 或 UTF-16 `AO` 头，并校验对应版本段 | `ArcFormats/Aoi/ArcBOX.cs` |
| DAI System | `*.pac` 命中完整 `DAI_SYSTEM_01000` | `ArcFormats/DaiSystem/ArcPAC.cs` |
| DDSystem | `*.dat` 命中 `DDP2` 或 `DDP3` | `ArcFormats/DDSystem/ArcDDP.cs` |
| DenSDK | `*.dat` 命中 `DAF1` 或 `DAF2` | `ArcFormats/DenSDK/ArcDAF.cs` |
| DxLib 8 | `*.bin` 命中字节 `44 58 08 00` | `ArcFormats/DxLib/ArcDX8.cs` |
| Ellefin Game System | 归档头命中 `EPK\x1A` 或 `EPK\x1E` | `ArcFormats/Ellefin/ArcEPK.cs` |
| Emon Engine | `*.eme`/`*.rre` 命中完整 `RREDATA ` | `ArcFormats/EmonEngine/ArcEME.cs` |
| Eushully System 3/4 | 存在 `*.alf`，且 `sys3ini.bin`/`sys4ini.bin` 命中 `S3IC`、`S3IN`、`S4IC` 或 `S4AC` | `ArcFormats/Eushully/ArcALF.cs` |
| Favorite View Point | `*.bin` 命中 `ACPXPK01` 或 `ACP_PK.1` | `ArcFormats/Favorite/ArcFVP.cs` |
| F&C MRG | `*.mrg` 命中 `mrg0` | `ArcFormats/FC01/ArcMRG0.cs` |
| FFA System | `*.arc` 命中 `M2TYPE_WAV`、`M2T_BMP` 或 `M2T_WORD` | `ArcFormats/Ffa/ArcFFA.cs` |
| G2 | `*.pak` 命中 `GCEX` | `ArcFormats/G2/ArcGCEX.cs` |
| GLib | `*.g`/`*.xp` 命中完整 `GML_ARC\0` | `ArcFormats/GLib/ArcG.cs` |
| Glib2 | `*.g2`/`*.stx` 命中固定加密头 `10 33 D3 47` | `ArcFormats/Glib2/ArcG2.cs` |
| GSS | 同名 `*.arc`/`*.bin` 组合，索引文件命中 `LSDARC V.100` | `ArcFormats/Gss/ArcARC.cs` |
| Hot Soup Processor | 独立 `*.dpm`/`*.bin`/`*.dat` 命中 `DPMX` | `ArcFormats/HSP/ArcDPM.cs` |
| Hypatia | `*.pak`/`*.dat` 命中完整 `HyPack` | `ArcFormats/Hypatia/ArcKogado.cs` |
| ISM | 归档头命中完整 `ISM ARCHIVED` | `ArcFormats/Ism/ArcISA.cs` |
| KaGuYa Script Engine | `*.arc` 命中 `WFL1`，或对应格式命中 `LIN2`、`LINK` | `ArcFormats/Kaguya/ArcKaguya.cs`、`ArcLIN2.cs`、`ArcLINK.cs` |
| KScript | 归档头命中完整 `SCRPACK1` | `ArcFormats/KScript/ArcKPC.cs` |
| Lambda | 归档头命中 `CLS_FILELINK` 或 `$LapH__` | `ArcFormats/Lambda/ArcCLS.cs`、`ArcLAX.cs` |
| Littlewitch/RepiPack | 归档头命中完整 `RepiPack` | `ArcFormats/Littlewitch/ArcDAT.cs` |
| Lucifen | 固定 `SCRIPT.LPK` 命中 `LPK1` | `ArcFormats/Lucifen/ArcLPK.cs` |
| NekoSDK/NekoPack4 | `*.pak` 命中完整 `NEKOPACK4` | `ArcFormats/NekoSDK/ArcPAK.cs` |
| Pajamas Adventure System | `*.dat`/`*.pak` 命中完整 `GAMEDAT PAC` | `ArcFormats/Pajamas/ArcGameDat.cs` |
| rUGP | `*.rio` 命中固定二进制签名 `CD 32 6E 59` | `ArcFormats/rUGP/ArcRIO.cs` |
| SAS5 | `*.sec5` 命中 `SEC5` | `ArcFormats/Sas5/ArcSec5.cs` |
| Selene | `*.pack` 命中 `KCAP`；扩展名用于避开 Leaf 的同签名格式 | `ArcFormats/Selene/ArcKCAP.cs` |
| SH System | `*.hxp` 命中 `Him4`、`Him5`、`SHS6` 或 `SHS7` | `ArcFormats/SHSystem/ArcHXP.cs` |
| Silky/AI6WIN | `*.ifl` 命中 `IFLS`，或 `*.mfg`/`*.mfm`/`*.mfs` 命中 `ALPF` | `ArcFormats/Silky/ArcIFL.cs`、`ArcMFG.cs` |
| SLG System | 资源归档命中完整 `SZS10__` | `ArcFormats/Slg/ArcSZS.cs` |
| Super NekoX | `*.gpc` 命中 `Gpc7` | `ArcFormats/SuperNekoX/ArcGPC.cs` |
| SVIU System | `*.pkz` 命中 `PKZ0` | `ArcFormats/Sviu/ArcPKZ.cs` |
| Tactics | `*.arc`/`*.adf` 命中完整 `TACTICS_ARC_FILE` | `ArcFormats/Tactics/ArcTactics.cs` |
| TamaSoft ADV System | `*.epk` 命中 `EPK ` | `ArcFormats/TamaSoft/ArcEPK.cs` |
| Tanaka engine family | `*.arc`/`*.bmx`/`*.scb`/`*.vpk` 命中 `ARCG` 并校验偏移 4 的版本值 | `ArcFormats/Tanaka/ArcARCG.cs` |
| Taskforce | `*.dat` 命中完整 `tskforce` | `ArcFormats/Taskforce/ArcDAT.cs` |
| GEM/vnengine | `*.axr` 命中 `AXRe` | `ArcFormats/VnEngine/ArcAXR.cs` |
| VNSystem | `*.vfs` 命中完整 `VFS File` | `ArcFormats/VnSystem/ArcVFS.cs` |
| Wild Bug | `*.wbp` 同时命中 `ARCFORM` 与偏移 8 的 ` WBUG ` | `ArcFormats/WildBug/ArcWBP.cs` |
| Yuka | `*.ykc`/`*.dat` 命中 `YKC0` | `ArcFormats/Yuka/ArcYKC.cs` |

下列 7 个也能由现有操作直接读取，但签名较短、词义过于通用，或只覆盖特定加密变体，因此首版保持实验状态：

| 引擎/系统族 | 直接证据 | 保持实验的原因 | GARbro 处理器 |
| --- | --- | --- | --- |
| AZ System | `*.arc` 命中 `ARC\x1A` 或两个固定加密头 | 标准头较通用，加密头只覆盖已知方案 | `ArcFormats/AZSys/ArcAZSys.cs`、`ArcEncrypted.cs` |
| ADVDX/BlackRainbow ADS | `*.ads` 命中固定加密头 `4E 51 D9 84` | 只覆盖 GARbro 已登记的加密变体 | `ArcFormats/BlackRainbow/ArcADS.cs` |
| DAC | 归档头命中 `DPK\0` | 只有三个可读字符 | `ArcFormats/Dac/ArcDPK.cs` |
| Foster | 归档头命中 `FA2\0` | 只有三个可读字符 | `ArcFormats/Foster/ArcFA2.cs` |
| Liar-soft | `*.xfl` 命中 `4C 42 01 00` | 短二进制头，缺少第二个独立静态证据 | `ArcFormats/Liar/ArcXFL.cs` |
| MNP | 归档头命中 `ARC!` | 标记词过于通用 | `ArcFormats/Mnp/ArcMMA.cs` |
| YOX ADV+++ | `*.dat` 命中 `YOX\0` | 只有三个可读字符 | `ArcFormats/Yox/ArcYOX.cs` |

本批已新增 52 个直接特征规则，其中 45 个为正式、7 个为实验；内置声明式引擎规则现为 74 条（66 正式、8 实验），资源版本为 `2026.08.25-1`。规则仍只在新增游戏分析、完整扫描或用户点击“重新检测”时执行；程序启动、规则保存和普通浏览不会自动重扫游戏库。资料驱动夹具覆盖处理器依据、合成正向、同扩展名错误签名、近似魔数、截断、复合证据和跨格式碰撞。当前电脑缺少真实样本时如实保留另一台电脑人工复测项，不提交商业资源。

### 13.5 GARbro ArcFormats 上游候选清单

GARbro 是资源格式浏览器，其 `ArcFormats` 同时包含游戏引擎/厂商格式、通用图片音频、压缩算法、平台格式和内部基础设施。GameShelf 只把它作为候选证据来源，不能因为 GARbro 存在某个处理器就直接断定游戏属于同名引擎，也不能从资源格式推导存档目录。

本清单固定到 GARbro `master` 提交 [`b09ee4570ccb1daf6ac56710ee8934dc0b8baeb0`](https://github.com/morkt/GARbro/tree/b09ee4570ccb1daf6ac56710ee8934dc0b8baeb0/ArcFormats)，上游提交时间为 2023-10-27，本地复核日期为 2026-08-25。后续调研必须记录具体处理器文件、稳定签名、文件名约束、结构校验、适用作品或厂商说明、误报风险和 GameShelf 状态；不得仅引用顶层目录名。

当前状态约定：

- **已实现且属于直接特征或已有专用检测器**：Artemis、CatSystem、Cmvs、Entis、Ethornell、KiriKiri、LiveMaker、Majiro、Malie、NitroPlus、NScripter、Qlie、RealLive、RenPy、ShiinaRio、Softpal、Unity、YuRis。
- **V0.3.2 已实现直接特征**：13.4 列出的 52 条规则；无法由当前操作直接表达的候选没有混入本批实施范围。
- **默认不作为引擎候选**：Adobe、DirectDraw、Lzma、Macintosh、Macromedia、Palette、PkWare、Properties、Psp、Resources、Strings、WebP 等明显的通用格式、平台或工程资源；若以后发现其中存在明确游戏运行时证据，必须重新记录理由后再调整。
- **其余目录**：统一视为“未形成直接规则”，不是已经承诺实现的规则。原因可能是厂商专属而非引擎、只有通用媒体格式、签名为零、必须解密/解析索引，或需要当前不存在的 `glob_magic_from_end` 等操作。

对 228 个目录逐项读取 `ArchiveFormat` 的 `Description`、文件头签名、扩展名和 `TryOpen()` 约束后，GARbro 源码至少在一个处理器中明确使用 `engine`、`system`、`scripter` 或等价运行时措辞的顶层目录共有 120 个：

- Abel、Actgs、ActiveSoft、Ads、AdvDx、AdvScripter、AdvSys、Aims、AIRNovel、AliceSoft、AnimeGameSystem、Aoi
- Artemis、AZSys、BlackCyc、BlackRainbow、CatSystem、Cmvs、Crowd、Dac、DaiSystem、DDSystem、DenSDK、DjSystem
- DxLib、Eagls、elf、Ellefin、Emic、EmonEngine、Emote、EntExec、Entis、Ethornell、ExHibit、FamilyAdvSystem
- Favorite、FC01、Ffa、Foster、FrontWing、G2、GameSystem、GLib、Glib2、GScripter、Gss、HCSystem
- HSP、Hypatia、Ikura、Irrlicht、Ism、Kaas、Kaguya、KiriKiri、KScript、Lambda、Leaf、Lilim
- Littlewitch、LiveMaker、Lucifen、Majiro、Malie、Marble、MnoViolet、Mnp、Moonhir、Musica、MyAdv、Nags
- Nejii、NekoSDK、Nexas、NitroPlus、Nonono、NScripter、NSystem、Origin、Pajamas、PalmTree、Primel、Qlie
- RealLive、RenPy、RPM、rUGP、Sas5、ScrPlayer、Selene、Seraphim、ShiinaRio、SHSystem、Silky、Slg
- Softpal、Speed、Stack、StudioEgo、StudioJikkenshitsu、SuperNekoX、Sviu、SysD、Tactics、Tamamo、TamaSoft、Tanaka
- Taskforce、Tmr-Hiro、Triangle、uGOS、Unity、VnEngine、VnSystem、WildBug、Will、YaneSDK、Yox、Yuka

这 120 个目录表示“源码中至少存在引擎/系统相关处理器”，不表示整个顶层目录只包含一种引擎，也不表示可以直接识别。Eushully、Liar 和 YuRis 的处理器说明没有使用上述英文关键词，但结合 `sys3ini.bin`/`sys4ini.bin`、XFL 与 YPF 的具体结构以及 GameShelf 既有规则，可另行确认为运行时相关；因此本轮得到 123 个引擎/系统相关格式族。其余 105 个不能仅凭顶层目录名判定为引擎，其中相当一部分只是厂商、作品、媒体中间件或通用归档格式。

在这 123 个相关格式族中，18 个已经由现有规则或专用检测器覆盖，52 个满足 13.4 的直接实现条件。像 FamilyAdvSystem、NSystem 虽有固定头但没有稳定文件名/扩展名，Nejii、Stack 虽有文件尾特征但当前没有 `glob_magic_from_end`，Will、Circus 则依赖结构解析；这些都不进入 13.4，避免为了扩充数量引入全目录枚举或新的解析能力。

GARbro `ArcFormats` 的 228 个顶层格式族完整索引如下。已经实现、已选中或暂缓的目录仍保留在索引中，避免后续上游复核遗漏：

- Abel、Abogado、Actgs、ActiveSoft、Adobe、Ads、AdvDx、AdvScripter、AdvSys、Ail、Aims、AIRNovel
- AliceSoft、Amaterasu、AnimeGameSystem、Ankh、Antique、Aoi、Apricot、Artemis、Astronauts、AZSys、Banana、Basil
- BeF、BellDa、Bishop、BlackCyc、BlackRainbow、BlueGale、Bonk、Bruns、C4、Cadath、CaramelBox、Carriere
- CatSystem、CDPA、Cherry、Circus、ClickTeam、Cmvs、Cri、Cromwell、Crowd、CsWare、Cyberworks、Dac
- DaiSystem、DDSystem、Debonosu、DenSDK、DigitalWorks、DirectDraw、DjSystem、Dogenzaka、DxLib、Eagls、elf、Ellefin
- Emic、EmonEngine、Emote、EntExec、Entis、Escude、Eternity、Ethornell、Eushully、ExHibit、FamilyAdvSystem、Favorite
- FC01、Ffa、FlyingShine、Foster、FrontWing、G2、GameSystem、GLib、Glib2、Gpk2、Groover、GScripter
- GsPack、Gss、HCSystem、Hexenhaus、HSP、Hypatia、Ice、Ikura、Illusion、Interheart、Ipac、Irrlicht
- Ism、Ivory、JamCreation、Kaas、Kaguya、Key、Kid、KiriKiri、Kiss、Kogado、KScript、Kurumi
- Lambda、Leaf、Liar、Lilim、Littlewitch、LiveMaker、Lucifen、LunaSoft、Lzma、Macintosh、Macromedia、MAI
- Maika、Majiro、Malie、MangaGamer、Marble、Masys、Mebius、MicroVision、Misc、Mixwill、MnoViolet、Mnp
- MokoPro、Moonhir、Morning、Mugi、Musica、MyAdv、Nags、Nejii、Nekopack、Nekopunch、NekoSDK、Nexas
- NitroPlus、Noesis、NonColor、Nonono、NScripter、NSystem、OneUp、Origin、Otemoto、Pajamas、Palette、PalmTree
- Pandora、Patisserie、PkWare、Primel、Propeller、Properties、Psp、Qlie、RealLive、RenPy、Resources、RiddleSoft
- Risa、Rits、RPM、rUGP、Sas5、ScenePlayer、Scoop、ScrPlayer、Selene、Seraphim、ShiinaRio、SHSystem
- Silky、Slg、Softpal、Software House Parsley、Sohfu、Speed、Stack、Strikes、Strings、StudioEgo、StudioJikkenshitsu、StudioSakura
- Succubus、SuperNekoX、Sviu、SysD、Tactics、Tail、Tamamo、TamaSoft、Tanaka、TanukiSoft、Taskforce、TechGian
- TechnoBrain、Tmr-Hiro、TopCat、Triangle、Ucom、uGOS、UMeSoft、Unity、Valkyria、Vitamin、VnEngine、VnSystem
- WebP、WildBug、Will、Winters、Xuse、YaneSDK、Yatagarasu、Youkai、Yox、Yuka、YuRis、Zyx

## 14. 后续工作

- 使用用户实际误判目录进行只读诊断，但只把合成最小回归样本加入仓库。
- 为 BGI、CatSystem2、YU-RIS 等正式规则补充更多变体与负面证据。
- 在具备对应作品的电脑上完成 V0.3.0 首批规则真实游戏矩阵，并根据脱敏诊断继续校准变体；低可信规则不因单个样本自动转正。
- 为厂商定制引擎保留手动自定义名称，不在缺乏证据时添加泛化规则。
- 引擎覆盖率与扫描准确率分别统计，避免用“未知引擎”误判为“漏扫游戏”。

## 15. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-12 | 完成专用检测器、声明式规则、证据展示和手动覆盖。 |
| 2026-08-13 | 修复嵌套 Unity、Siglus 汉化文件与手动主程序联动。 |
| 2026-08-13 | 将 MTool 调研范围与 GameShelf 的静态识别边界写入固定文档。 |
| 2026-08-13 | 将 VX Ace 解包结构、MV 双布局回归、Artemis 可变名称 PFS、Unreal 正式检测及重扫手动保护列为 V0.1 发布门槛。 |
| 2026-08-13 | 完成 VX Ace 解包结构、MV 双布局回归、Artemis 可变名称 PFS、Unreal 正式检测及重扫手动保护。 |
| 2026-08-16 | 冻结轻量版损坏 `engines.yaml` 验收未通过：解析错误当前会中止 `applicationBootstrap`，未按设计只禁用声明式检测器；根因定位到应用构建期间的未隔离同步规则加载，恢复原规则后 smoke 重新通过。 |
| 2026-08-16 | 确认损坏规则修复设计：仅现有普通规则文件的解析/结构错误在应用构建层降级为内置检测器并记录 WARNING；缺失资源和命令行诊断仍严格失败，V0.1 不新增降级横幅。 |
| 2026-08-16 | 完成损坏声明式规则降级：损坏 YAML/结构错误记录 WARNING 后仅启用内置检测器，缺失文件仍严格失败且不会先创建数据库；新增 3 项回归后 Python 558 项、前端 88 项及全部静态、类型、构建和源码 smoke 门禁通过。随后重建双版本候选，隔离轻量版冻结副本在损坏规则下退出 0、保留应用构建与内置检测器、记录降级 WARNING 且不生成启动错误日志，恢复规则后再次通过。 |
| 2026-08-21 | 确认 V0.3 双规则库与共享基础层设计：V0.3.0 先校准 7 个现有实验引擎并新增 LiveMaker、CMVS/CVNS、Godot；证据充分才转正式，真实游戏复测后置为人工清单；内置规则完成后再实施独立的引导式自定义规则编辑器。 |
| 2026-08-21 | 完成 V0.3.0 内置引擎规则扩展：6 个旧实验规则转正式，SoftPal/AmuseCraft 保持实验，新增 LiveMaker、CMVS/CVNS、Godot 三个正式规则；建立共享元数据、证据台账、正反样本门禁和 `--sanitized` 脱敏诊断，真实游戏不足项继续列入人工矩阵。 |
| 2026-08-21 | V0.3.0 收口通过 Python 963 项、前端 197 项、Ruff、mypy、Vue 类型检查、115 模块生产构建和隔离 schema 4 源码 smoke；真实游戏矩阵与 SoftPal/AmuseCraft 后续校准仍未伪装为已验收。 |
| 2026-08-23 | 确认 V0.3.x 引擎规则编辑器设计：独立响应式规则管理工作台、内置只读与本地禁用、用户规则一文件一规则、引导式表单、只读 YAML 和本地游戏测试、实验/用户验证状态回退、不可变任务快照及完整候选校验均作为实施边界。 |
| 2026-08-23 | 完成 V0.3.1 引擎规则编辑器源码：内置只读测试/复制/启停、用户规则 CRUD、8 种受限证据操作、验证状态回退和任务快照均已实现；真实游戏矩阵继续后置。 |
| 2026-08-25 | 用户确认 V0.3.1 规则管理真实窗口人工验收全部通过；逐项复核固定提交下 GARbro ArcFormats 228 个顶层目录，识别出 120 个由源码明确描述为引擎/系统相关的目录及 3 个需结合结构确认的运行时族；V0.3.2 只纳入现有安全操作可直接表达的 45 个正式与 7 个实验候选，不再在实施表混列暂缓项。 |
| 2026-08-25 | 完成 V0.3.2 第二批内置引擎规则：新增 45 条正式与 7 条实验规则，内置规则总数达到 74 条；资料驱动夹具覆盖全部签名变体、近似魔数、截断、复合证据与 KCAP/NEKOPACK 碰撞，真实商业游戏样本矩阵继续后置。 |

## 16. 本次归并来源

本文已经吸收 [`2026-08-12-主程序排序与引擎检测修复.md`](../../历史归档/2026-08/2026-08-12-主程序排序与引擎检测修复.md) 中与引擎检测有关的当前有效结论。
