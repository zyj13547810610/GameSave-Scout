模块 07 完成后，建议把“批量封面向导”作为模块 03 的独立后续增强，不纳入当前便携发布计划。
核心界面采用“左右分栏工作台 + 统一候选池”：
左侧显示无封面游戏队列、处理进度和状态。
右侧显示当前游戏来自不同渠道的全部候选。
点击“采用并下一个”后立即保存，并自动进入下一款游戏。
支持中途退出；再次进入时根据现有封面状态继续。
默认只显示无封面游戏，可选择包含已有封面的游戏进行替换。
候选来源包括：
批量选择封面文件夹，根据游戏标题、安装目录名和图片文件名匹配。
主动扫描游戏目录中的浅层图片，不深度遍历、不解包资源。
剪贴板粘贴和图片拖放。
可选在线搜索。
所有候选进入同一个画廊，显示来源和匹配度。精确匹配优先，模糊匹配不自动采用；冲突必须人工处理。
在线功能：
全局默认关闭。
开启后支持“搜索当前游戏”和“批量搜索在线候选”两个入口。
批量搜索前显示游戏数量并要求确认。
支持进度、取消、限速和单项失败隔离。
只获取候选，不自动设置封面；最终仍逐个审核确认。
第一版只接入 VNDB；其公开查询大多不要求认证，并支持标题、别名、发行标题及封面字段。VNDB 官方 API
SteamGridDB 等需要 API Key 的普通游戏来源留作后续增强。
在线候选正常显示，不额外模糊成人或暴力内容标记。
数据与安全边界：
只有用户点击采用时才调用现有封面保存服务。
每个游戏独立保存，不新增批处理数据库事务。
本地原图只读，不移动、不覆盖、不删除。
在线候选先进入临时目录，未采用内容及时清理。
不向在线服务发送安装路径、数据库或本地文件。
单个来源或单个游戏失败不影响已经完成的项目。
第一版不做自动采用、深度游戏资源扫描、资源解包、裁剪编辑器、SteamGridDB/IGDB 接入或新的批处理数据库结构。